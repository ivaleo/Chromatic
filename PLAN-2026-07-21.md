# План доработок Chromatic — 21.07.2026

По итогам аудита `AUDIT-2026-07-21.md`. Порядок = приоритет. Для каждого пункта —
критерий приёмки (КП). Версии всех трёх пакетов поднимаются до 1.1.0.

## Этап 1. combigeo (C++ ядро)

1.1. **`transition` в базисе пользователя** (аудит B-1, критично).
  - Новая функция `hnf_of(C)` (целочисленная HNF заданной матрицы, та же конвенция:
    верхнетреугольная, диагональ > 0, наддиагональ приведена по модулю диагонали).
  - В `find_optimal`: у победителя пересчитать C = sub_basis·B⁻¹ (B — входной базис),
    проверить целочисленность (|x−round(x)| < 1e-6, иначе исключение), выдать
    `transition = hnf_of(C)`.
  - CLI печатает и `sub_basis`.
  - КП: новый тест — решётка со скошенным базисом ({{3,1},{2,1}} и т.п.):
    `transition × входной базис` порождает ту же подрешётку, что `sub_basis`
    (целочисленные взаимные координаты + равенство det); все старые тесты зелёные.

1.2. **Сертифицированные релевантные векторы** (B-4, критично для n ≥ 5).
  - Вместо окна ±3: для каждого из 2ⁿ−1 нетривиальных классов Λ/2Λ — минимумы класса
    точным CVP-перебором (сдвинутый Шнорр–Ойхнер по решётке 2Λ, стартовый радиус —
    по Бабаи); класс релевантен ⟺ ровно два минимума (±v) с допуском kEps.
  - Self-check ячейки: соотношение Эйлера и центральная симметрия множества вершин;
    нарушение — исключение.
  - КП: все пины f-векторов прежние (Z2 Z3 FCC BCC Z4 D4 hex + A5*-пермутоэдр
    [720,1800,1560,540,62]); Z4 строит ячейку по 8 фасетам (быстрее на порядки);
    добавлен smoke-тест n=5 (Z5: f=[32,80,80,40,10], D5).

1.3. **Масштабная инвариантность** (B-3).
  - Входной базис нормируется s = |det B|^{1/n} во всех точках входа
    (`voronoi_cell`, `find_optimal(_range)`, `min_color_distance`); выходы (vertices,
    diameter, min_distance, sub_basis, witness) масштабируются обратно; проверка
    вырожденности — относительная.
  - КП: hex ×1e8 → f=[6,6], diam масштабируется точно; FCC ×1e-6 работает; все
    старые пины без изменений; d — инвариант.

1.4. **Валидация python-`lll_reduce`** (B-2): `validated_dim` в биндинге.
  - КП: рваный список → `ValueError`/`invalid_argument`, не падение (тест).

1.5. **Быстродействие** (B-5): в `find_optimal_range` — одна ячейка и общий кэш на
  диапазон; в `min_color_distance` — одно LLL-приведение подрешётки (флаг
  `assume_reduced` у `shortest_vector`/`vectors_within`).
  - КП: `find_optimal_range(D4, 2, 50)` ускоряется ≥ 25%, результаты бит-в-бит.

1.6. Мелочи: GJK — контроль зазора двойственности на всех выходах (исключение при
  превышении); прогресс — в stderr; `py::arg("from")` → `frm` (+ `use_cache` в
  range-биндинге); `__version__` у модуля; пресет `A2` в CLI; фикс пути и
  importorskip в tests/python; LICENSE (MIT).

## Этап 2. voronoi4d (эталон)

2.1. **Полный перебор HNF** (C1, критично): `compute_factorizations` перечисляет ВСЕ
  упорядоченные диагонали (рекурсия по делителям), не только неубывающие.
  - КП: суммарное число матриц на индекс = классической формуле Σ d₂·d₃²·d₄³
    (пины: k=2 → 15, k=12 → 6200, k=49 → 140050); тест-фиксатор старого поведения
    переписан.

2.2. **Граница достаточности кандидатов** (C2, критично): новый модуль
  `enumeration.py` (точный сферический перебор, numpy); кандидаты — все |v| <
  D_текущ + diam; параметр `limits` объявлен устаревшим (игнорируется с warning).
  - КП: флагман D4/49 = 1.0801234497 воспроизводится; тест полноты на «вытянутой»
    подрешётке, где бокс ±2 терял вектор.

2.3. **Валидация ячейки** (C3, критично): симметричное облако range(−3,4); проверки
  vol(V₀) ≈ |det(grid)| (ConvexHull) и центральной симметрии вершин; при провале —
  ValueError с советом применить `lll_reduce`.
  - КП: скошенный базис из аудита (vol 0.295 vs det 0.02) теперь даёт исключение;
    все валидные пины строятся.

2.4. **dist_to_s / nearest_faces** (M1, M4): точка внутри ячейки → 0.0; полный скан
  фасет вместо недоказанного отсечения по ближайшим вершинам (эталону — строгость);
  `set.intersection` больше не используется; нулевой вектор исключён из кандидатов
  (устранена скрытая зависимость от inf); rank-check нормали Face2D по сингулярным
  числам.
  - КП: 0 расхождений с QP-оракулом на случайных точках (тест с фикс. сидом);
    dist_to_s(0) = 0.0.

2.5. **fpylll-путь** (M2): precision по умолчанию 12; передача delta; при
  непредставимости — фолбэк на python-LLL; тест эквивалентности бэкендов по Грам-
  инвариантам.

2.6. **threshold-семантика** (M3): фильтры |v| ≤ t·diam и ранний выход — через
  переданный threshold; согласование max_len/vor4 (M5) — проверка на входе.

## Этап 3. chromatic (фасад)

3.1. Общий модуль `_hnf.py`; оба адаптера возвращают `transition` в базисе
  пользователя; для voronoi4d появляется `sub_basis` (= H·LLL(grid), пересчитан в
  объемлющие координаты).
  - КП: тест — оба бэкенда, D4/16: transition-подрешётки совпадают как решётки.

3.2. Тесты: пин combigeo D4/49 d=1.0801234497; guard «нет ни одного бэкенда — FAIL»;
  кросс-тест на допустимом индексе (D4/16, оба бэкенда); m8 (генератор), m9
  (вердикт с tol).

3.3. Версии/доки: 1.1.0, USAGE — fpylll-семантика, рамка transition, слоты None.

## Этап 4. Актуализация документации (параллельный агент)

- `voronoi/docs/article.tex` — переписать: компилируемый, одна согласованная
  нормировка d = D/diam (без ε), леммы с точными формулировками, описание алгоритма
  как в коде (мин. по всем кандидатам с границей достаточности), таблицы результатов.
- `articles/README.md` — каталог статей с идентификацией и пометкой дубликатов.
- `voronoi/README.md` — формулы (множитель 2), шаг 5 алгоритма, судьба `limits`.
- `combigeo/README.md`/`docs/USAGE.md` — мёртвый путь → ../voronoi, «невырожденном» →
  «вырожденном», семантика transition (базис пользователя), пресет A2.
- корневой `README.md` — раздел «Результаты и воспроизведение» (AUDIT, RESULTS,
  audit-data), правка «try/except ImportError» → find_spec.
- `chromatic/docs/USAGE.md` — как в 3.3.

## Этап 5. Расчётные кампании (после фиксов; фоново)

5.1. Регрессия: повторить sweep аудита, сравнить с audit-data/*.json (бит-в-бит по d).
5.2. ℝ²: A2 и Z2, k = 2..40 — полная лестница.
5.3. ℝ³: Z3/FCC/BCC k = 2..64; семейство Иванова diag(1,b): сетка b ∈ (−1,0),
  k = 13..32 — поиск НОВЫХ интервальных оценок сверх его таблицы.
5.4. ℝ⁴: D4, A4*, K3,3, 111− k = 2..100 — лестницы допустимых k и ширин.
5.5. ℝ⁵: исчерпывающий перебор A5*, k=140 (запущен до фиксов на текущем ядре;
  повторная сверка витнесса после фиксов) + точное d.
5.6. ℝ⁶–ℝ⁹: python-QP конвейер — E7*/1372 (C7), A9*/17253 (C9) из Arman et al.;
  прогноз по закону (3+ω) для K12 (отрицательный) и Λ24 (гипотеза).
5.7. Все численные d — с точной рациональной реконструкцией d².

## Этап 6. Итоговый отчёт

`RESULTS-2026-07-21.md`: сводные таблицы верхних оценок χ(ℝⁿ,[1,ℓ]) по n = 1..9, 24
(опубликованное vs наши точные константы vs новое), лестницы d(k) по размерностям,
таблица верификации статей + эррата, раздел «новые результаты», методология и
ограничения (граница d=1, статус сертификатов).

## Вне объёма (решение пользователя)

- git init в корне воркспейса и remotes для chromatic/combigeo (структурное решение VCS);
- удаление файлов-дубликатов в articles/ (помечены в articles/README.md);
- Aut-редукция HNF-перебора и кампания «побить 343 в n=6» (следующая веха).
