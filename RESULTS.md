# Хроника вычислительных кампаний

## Текущее состояние (сводка)

Лучшие известные проекту верхние оценки; каждая строка — χ(ℝⁿ,[1,ℓ]) ≤ k при
ℓ < d. Статусы: [Т] теорема, [С] точный рациональный сертификат, [Ч] численный
сертификат (плавающая точка). Подробности — в секциях хроники ниже и в статье.

| n | k | d | статус | механизм | статья |
|---|---|---|---|---|---|
| 2 | 7 | 1.3229 | классика | A₂/7 | §2 |
| 2 | 8 | 1.4414 | **[С]** | нерешёточная мозаика, 8 орбит | §10 |
| 2 | 15 | 2.2815 | **[С]** | нерешёточная мозаика, 15 орбит | §10 |
| 3 | 15 | 1.0266 | **[С]** | деформация ОЦК в семействе G(α), α=16/51 (у Кулсона ровно 1) | §6 |
| 4 | **45** | 1.0163 | **[С]** | решётка общего положения | §4 |
| 5 | **132** | 1.0109 | **[С]** | решётка общего положения | §5 |
| 6 | 343 | 1.0801 | АБПР; ширина [Т] | (3+ω)E₆* | §6 |
| 7 | **1029** | 1.0329 | **[Ч]** | ламинирование E₆*/343 (1323 — [С]) | §7 |
| 8 | 2401 | 1.0801 | АБПР; ширина [Т] | (3+ω)E₈ | §6 |
| 9 | **7203** | 1.0166 | **[Ч]** | ламинирование E₈/2401; лестница до √(7/6) | §7 |
| 10 | **28812** | 1.0433 | **[Ч]** | A₂-слой над E₈/2401, полосный сертификат | §8 |
| 10 | 45619 | 1.0070 | **[Т]** | произведение E₈/2401 ⊕ aA₂/19 | §8 |
| 11 | 3¹¹ | 1.1166 | [Ч] | башня ламинаций E₈ | §7 |
| 12 | 3¹² | 1.2247 | **[Т]** | Γ = 3K₁₂ (Коксетер–Тодд) | §7 |
| 24 | 7¹² | 1.0801 | индекс АБПР; ширина **[Т]** | (3+ω)Λ₂₄, планарная теорема | §6 |
| 25 | 4·7¹² | 1.016 | **[Т]** | произведение Λ₂₄ × ℤ | §8 |
| 26 | 19·7¹² | 1.0070 | **[Т]** | произведение Λ₂₄ × aA₂ | §8 |

Кандидаты без доверенного сертификата: 21609 в ℝ¹⁰ (полосный сертификат
проходит с запасом 5·10⁻⁴ — нужна рационализация), 38416 в ℝ¹⁰ (измерено
1.0616, сертификат не запускался).

## Кампания 10.08.2026: окна интервальных сертификатов и точная ширина ℝ³/15

Две правки по замечаниям к версии 5 статьи, обе — доказательная база.

**Окно сертификатов 132/1323 исправлено** (`campaigns/certificate_window_fix.py`).
Старый генератор перечислял короткие векторы в окне ‖v‖ < 4R (цель d > 1), а
интервалы ℓ = 101/100 и 1007/1000 требуют окна ‖v‖ < 2(1+ℓ)R. Пересчёт:
радиусы покрытия подтверждены повершинным точным аудитом (720 и 39 296 вершин),
окно перечислено точно (бокс-сертификат с индивидуальными границами координат +
сверка с независимым C++-Финке–Похстом), добавились ровно по одной паре:
±(−2,2,−2,2,2) с D/diam = 1.044187 (ℝ⁵) и ±(2,−1,0,0,0,0,1) с D/diam =
1.189055 (ℝ⁷) — обе выше минимума, минимумы не изменились (1.010897714549 и
1.007032309006). Артефакты теперь содержат 38 и 72 вектора с KKT-сертификатами
каждого и блоки `certified_interval` (у 1323 его не было). Тексты теорем 2 и 3
статьи, опережавшие артефакты, теперь ими покрыты.

**Точная ширина k=15 в ℝ³** (`campaigns/dim3_k15.py`,
`results/dim3_k15_certificate.json`). Конструкция Кулсона (ОЦК/15) вложена в
однопараметрическое семейство G(α) = [[1,−α,2α−1],[−α,1,−α],[2α−1,−α,1]] с
фиксированной подрешёткой T = [[1,0,11],[0,1,7],[0,0,15]]. Символьное решение
KKT-систем связывающих орбит даёт замкнутые формы

    D²₍₃,₂,₂₎ = (4α²+3α+1)/(α+1),  D²₍₂,₁,₋₁₎ = 2(5α²−6α+2)/(1−α),  diam² = 2−α,

откуда у Кулсона (α=1/3) ширина **ровно 1** (вырожденный интервал; проверено и
полным точным аудитом), а максимум семейства — корень кубики
14α³−3α²−10α+3 = 0: α* = 0.3136953…, d* = 1.0265986. Лучший численный оптимум
по всем шестипараметрическим формам (o2_r3: 1.0265930) его не превосходит —
семейство, по-видимому, содержит глобальный оптимум k=15. В рациональной точке
α = 16/51 вся цепочка проверена в точной арифметике: d² = 1586/1505, итог
**χ(ℝ³,[1,ℓ]) ≤ 15 при ℓ ≤ 513/500 = 1.026 [С]** — прежняя запись ℓ ≤ 1.0266
была численной и завышенной (замечание №4 к версии 5).

## Кампания 08.08.2026: полурешётчатые (нерешёточные) раскраски плоскости

Вопрос: окупается ли нерешёточная свобода класса мозаик §10 хоть где-нибудь?
Ответ: да — ровно на индексах, запрещённых решёточной схеме арифметикой, и две
такие ступени сертифицированы точно. Полный отчёт:
[journal/SEMILATTICE-2026-08-08.md](journal/SEMILATTICE-2026-08-08.md).

**Метод.** Конфигурация P = ⋃ᵢ(Γ + tᵢ): решётка периодов Γ и N свободных
сдвигов; куски — лагерровы ячейки P, цвет — орбита узла. Это срез класса
степенных мозаик (`core/power_coloring.py`) с акцентом на арифметику: подобная
подрешётка в плоскости существует лишь при индексе-норме ℤ[ω] (числа Лёшиана
1, 3, 4, 7, 9, 12, 13, 16, 19, 21, …), поэтому классическая лестница ширин
проваливается на каждом не-норменном N — **арифметическая квантизация**.

**Точный сертификатор** (`core/semilattice_cert.py`): всё в координатах базиса
с рациональным грамианом, ни одного извлечения корня; вершины ячеек — решения
рациональных 2×2 систем, diam²/sep² — сравнения дробей; точно проверяются
полнота оболочки и замощение (сумма площадей = ковообъём). Валидация: семь
классических ступеней воспроизведены точными d² = 3/4, 7/4, 3, 19/4, 27/4,
31/4, 37/4.

**Два новых сертифицированных результата** (не-норменные N, конфигурации CMA-ES
+ Нелдер–Мид, рационализация со знаменателем 15000):

    χ(ℝ²,[1,ℓ]) ≤ 8    при ℓ ≤ 1.441350343   (классика 7/5 = 1.4)
    χ(ℝ²,[1,ℓ]) ≤ 15   при ℓ ≤ 2.281534317   (классика 2.186607)

Проверка тройная: независимое перечисление вершин, `power_coloring.evaluate`
(совпадение до 10⁻⁹), Монте-Карло по определению (9–17 млн пар). Компактные
сертификаты со знаменателем 20: d = 1.424440243 и 2.219896000 — читаются
глазами (`results/semilattice_cert_N{8,15}{,_small}.json`).

**Сверка с литературой (08.08, после кампании).** Плоский интервальный случай
имеет собственную литературу, и эффект квантизации в ней известен: де Грей и
Партс (Geombinatorics 32/2, 2022) обгоняют решёточную схему неправильными
шестиугольниками ровно на не-лёшиановых k, а Партс (Geombinatorics 32/4, 2023;
arXiv:2303.14722) мозаиками из плиток разных форм достигает **численно**
d ≈ 1.444157 при k=8 и d ≈ 2.346969 при k=15 — сильнее наших значений. Поэтому
наши ступени — **не рекорды, а первые (насколько нам известно) точные
рациональные сертификаты** в этом классе; сертификация конфигураций Партса тем
же конвейером — прямая следующая задача. Таблица решёточной лестницы Партса
(Table 2) независимо совпадает с нашей `results/ladder2d.json`.

**Отрицательное: проставок ℝ¹⁰ устойчив.** Мозаичная свобода не отыгрывает
плоские индексы 17/18 (нужно d ≥ √7 = 2.6458): минимум максимального диаметра
по 18-точечным конфигурациям — 0.2853656 при пределе 0.2847561 (промах 0.21 %),
потолок d ≲ 2.638; поиск ширины упирается в 2.5981 — значение решётки k=16.
Оценки 45619 и 28812 устойчивы к нерешёточной свободе; квантизация в этом окне
не связывает.

**Артефакты:** `core/semilattice.py`, `core/semilattice_cert.py`,
`campaigns/planar_semilattice.py`, `results/semilattice_ladder2d.json`,
`results/semilattice_room.json`, `tests/test_semilattice.py` (13 тестов).

## Кампания 07.08.2026 (ночь): мозаичные раскраски, пол 2ⁿ, цена решёточности

Ревизия метода целиком. Вопрос: весь проект красит ячейки Вороного решётки —
сколько стоит эта решёточность и где вообще кончается метод? Ответ получен в
трёх частях; **новых верхних оценок эта кампания не дала — она объяснила,
почему их не даёт и поиск**.

### 1. Степенные мозаики — строго более широкий класс (`core/power_coloring.py`)

Решётка периодов `Γ`, `k` узлов `tᵢ` и весов `wᵢ`; узел `tᵢ + g` (`g ∈ Γ`)
получает вес `wᵢ`; берём **степенную (лагеррову) диаграмму** этого набора.
Ячейки выпуклы, замощают ℝⁿ, ячейка узла `tᵢ + g` есть `Pᵢ + g`. Цвет — номер
узла, цветов `k`. Родительской решётки `Λ` нет: узлы **не обязаны** образовывать
группу, ячейки разных цветов **не обязаны** быть конгруэнтны.

Две точки одного цвета лежат либо в одной ячейке, либо в `Pᵢ+g` и `Pᵢ+g'`; обе
выпуклы, значит реализуемые расстояния заполняют отрезок от `dist(g−g', Pᵢ−Pᵢ)`.
Отсюда

```
Δ = maxᵢ diam Pᵢ,   gap = minᵢ min_{0≠g∈Γ} dist(g, Pᵢ − Pᵢ),   d = gap/Δ
χ(ℝⁿ, [1, d]) ≤ k   при d ≥ 1
```

При `w = 0` и узлах `Λ/Γ` диаграмма становится диаграммой Вороного `Λ`,
`Pᵢ − Pᵢ = 2V₀`, и `dist(g, 2V₀) = 2·dist(g/2, V₀) = D(g)` — прежний критерий
**дословно**. Валидация (`campaigns/power_tilings.py`,
`results/power_tilings.json`): замкнутые формы `A₂/7 → √7/2`, `A₂/9 → √3`,
`ℤ³/27 → 2/√3`, `A₃*/27 → 2/√(5/3)`, `D₄/81 → √2`, `A₄*/81 → √2` воспроизведены
с ошибкой < 10⁻⁹, чемпионы работы — `k=45 → 1.0163393`, `k=44 → 0.9903509` —
до последнего знака.

`gap` считается алгоритмом Вульфа о точке минимальной нормы в политопе: линейный
оракул над множеством разностей вершин `{g − a + b}` (никогда не выписываемым)
есть пара `argmax/argmin` скалярного произведения, а каждая итерация предъявляет
разделяющий слой, дающий **нижнюю** оценку `dist ≥ ⟨g,e⟩ − width(P,e)`. Поэтому
`d` никогда не завышено. Грабли: Франк—Вулф сходится сублинейно и на `k=44`
занижал 0.99035 до 0.98872 даже за 60 итераций (900 итераций → 0.99026);
Вульф даёт точное значение за 0.18 с. Вторые грабли: перечисление точек решётки
обязано идти по LLL-приведённому базису — на скошенном базисе индекса 15 одна
оценка стоила 2.1 с вместо 0.14 с.

### 2. Теорема о поле 2ⁿ (`core/paradigm_floor.py`)

> Пусть ℝⁿ замощено выпуклыми плитками диаметра < 1, замощение `Γ`-периодично,
> плитки одного цвета попарно удалены больше чем на 1, и `P₁…P_k` — плитки
> одного периода. Тогда `k ≥ (1 + θ^{1/n})ⁿ ≥ 2ⁿ`, `θ = ωₙ2⁻ⁿ / maxᵢ vol Pᵢ ≥ 1`.

Доказательство в три строки: тела `Pᵢ + g + B(0,½)` при `g ∈ Γ` попарно не
пересекаются ⇒ `det Γ ≥ vol(Pᵢ ⊕ B(0,½))`; плитки точно заполняют период ⇒
`det Γ = Σᵢ vol Pᵢ ≤ k·V`; Брунн—Минковский и изодиаметрическое неравенство
(`V ≤ ωₙ2⁻ⁿ` при `diam ≤ 1`) замыкают. Равенство требует, чтобы плитка была
шаром, — недостижимо.

Теорема не знает ни о какой решётке, поэтому накрывает **весь** новый класс.
Для решёточных раскрасок пол Кулсона `2ⁿ⁺¹−1` сильнее; наш — то, что остаётся
без родительской решётки. Следствие: весь запас мозаичного принципа против
`3ⁿ` Лармана—Роджерса — множитель `(3/2)ⁿ`, и **никакое обобщение мозаичного
типа не даст асимптотики лучше 2ⁿ**.

### 3. Почему нерешёточная свобода не окупается

Назовём выпуклое тело `P` допустимым при `diam P ≤ 1` и `dist(g, P−P) ≥ 1` для
всех `g ∈ Γ\{0}`. Условие **не упоминает ни остальные плитки, ни цвет** и
наследуется подмножествами. Поэтому при фиксированной `Γ`

```
k = det Γ / (средний объём плитки) ≥ det Γ / vol P*,   P* — допустимое максимального объёма
```

и всякая раскраска, достигающая границы, состоит из копий одной формы — то есть
из ячеек Вороного. Дополнительная свобода может окупиться только через
**реализуемость замощения**, никогда — через условие на отдельную плитку.

Численная проверка: CMA-ES по всем параметрам (решётка периодов, узлы, веса)
сажался **ровно** в известный оптимум.

| случай | цветов | параметров | итог |
|---|---|---|---|
| ℝ², шестиугольная A₂ | 7 | 21 | 1.3228757 → без улучшения |
| ℝ³, ОЦК (оптимум Кулсона) | 15 | 62 | 1.0000000 → без улучшения |
| ℝ⁴, чемпион работы | 45 | 230 | 1.0163393 → без улучшения |
| ℝ⁴, лучшее при k=44 | 44 | 225 | 0.9903509 → без улучшения |

Калибровка силы оптимизатора: тот же CMA-ES со **случайных** стартов доходит в
ℝ² при k=7 лишь до 1.2396 (истина 1.3229) — значит отрицательный результат со
случайных стартов был бы слабым свидетельством. Здесь старт — сам чемпион,
утверждение локальное и потому надёжное. Свободный поиск при ℝ³/k=14 (решёточный
предел 0.7746, нужно 1.0) дошёл до 0.570, при ℝ²/k=6 — до 0.966: пробить не
удалось, как и должно быть.

Механизм локальности: в точке Вороного все `k` плиток конгруэнтны, поэтому все
активные ограничения (максимум диаметра и минимум зазора) достигаются
одновременно на каждой плитке; шевеление весов увеличивает объём части плиток,
а с ним диаметр, и уменьшает зазор.

### 4. Точный учёт метода и ландшафт запаса

Условие `D(v) ≥ 2R` для `v ∈ Γ` есть в точности `Γ ∩ int 2C = {0}` для
**округлённой ячейки** `C = V₀ ⊕ R·B`, то есть `Γ` — упаковочная решётка тела
`C`. Отсюда тождество

```
k = F(Λ) / δ_C(Γ),    F(Λ) = vol(V₀ ⊕ R·B)/det Λ ≥ (1 + Θ^{1/n})ⁿ
```

Родителю нужно быть хорошим **покрытием** (малое `Θ`), подрешётке — хорошей
**упаковкой** округлённой ячейки. Все конструкции проекта берут `Γ = αΛ`,
подобную `Λ`, и требуют обоих качеств от одной решётки.

| n | цветов | k^{1/n} | родитель | ρ | Θ | k/F_min | k/2ⁿ |
|---|---|---|---|---|---|---|---|
| 2 | 7 | 2.646 | A₂ | 1.155 | 1.21 | 1.6 | 1.8 |
| 3 | 15 | 2.466 | A₃* | 1.291 | 1.46 | 1.5 | 1.9 |
| 6 | 343 | 2.646 | E₆* | 1.414 | 2.65 | 3.2 | 5.4 |
| 8 | 2401 | 2.646 | E₈ | 1.414 | 4.06 | 4.5 | 9.4 |
| 10 | 45619 | 2.924 | E₈×A₂ | — | — | — | 44.5 |
| 11 | 3¹¹ | 3.000 | башня | 1.791 | — | — | 86.5 |
| 12 | 3¹² | 3.000 | K₁₂ | 1.633 | 17.8 | 28.2 | 129.7 |
| 24 | 7¹² | 2.646 | Λ₂₄ | 1.414 | 7.90 | 287 | 825 |

`Θ` выведена из точных `(λ₁², R², det)` и сверена с независимыми замкнутыми
формами `2π/3√3`, `π⁴/24`, `π¹²2¹²/12!` до 10⁻¹². Столбец `k/F_min` — это
`1/δ_C`, потеря на упаковке округлённой ячейки; она почти всюду близка к
`1/δ_max(n)`: при n=3 это 1.5 против 1.35, при n=8 — 4.5 против 3.94, при n=12 —
28.2 против 20.2. **Вывод для планирования: оставшийся запас метода — множитель
1.4–2 в размерностях 10–12, а не порядки; в ℝ³ и ℝ⁸ рекорды стоят в 10–15 % от
реалистичного пола.**

### 4b. Куда ещё можно целиться (второй, более сильный пол)

Инрадиусная лемма `D(v) ≤ |v| − λ₁(Λ)` превращает допустимость `D(v) > 2R` в
`λ₁(Γ) > 2R + λ₁(Λ)`; решётка с таким минимумом — упаковка шарами радиуса
`λ₁(Γ)/2`, откуда `k ≥ ωₙ(R + λ₁(Λ)/2)ⁿ / (δ_max(n)·det Λ)`. Обычно этот пол
сильнее объёмного (`core/paradigm_floor.py: index_window`, `open_windows`).

| родитель | n | λ₁(Γ) ≥ | пол по упаковке | пол по объёму | рекорд | запас |
|---|---|---|---|---|---|---|
| A₃* (ОЦК) | 3 | 2.291 | 11 | 10 | 15 | 1.36 |
| E₈ | 8 | 3.414 | 1154 | 532 | 2401 | 2.08 |
| K₁₂ | 12 | 5.266 | 111 019 | 18 826 | 3¹² | **4.79** |

Строки n=3 и n=8 строги (плотнейшие упаковки доказаны Хейлзом и Вязовской);
строка K₁₂ **условна** — оптимальность K₁₂ в размерности 12 открыта.

Читается так: подрешётка `Γ ⊂ K₁₂`, улучшающая `3¹²`, обязана иметь `λ₁² ≥ 28` и
индекс в окне `[111019, 531441]` — почти пятикратный запас, самый большой из
всех родителей. Эйзенштейнова `(3+ω)K₁₂` с индексом `7⁶ = 117649` лежит чуть
выше пола, но выбывает по другой причине: её `D_min` равно ровно планарному полу
`√(7/3)λ₁ = 3.055` при нужных `3.266`. Лестница подобий в этом окне пуста
(эйзенштейновы нормы идут 7, 9, 12, 13, …), поэтому целиться надо в
**ℤ[ω]-подмодули K₁₂ с произвольной нормой определителя** — это единственное
направление, на которое указывает учёт.

### 5. Где кончается эйзенштейнова лестница

Для подобной `Γ = αΛ` вся допустимость — одно неравенство на `ρ = 2R/λ₁`:
`ρ ≤ 2` при `α = 3` (цветов `3ⁿ`) и `ρ ≤ √(7/3) = 1.52753` при `α = 3+ω`
(цветов `7^{n/2} = 2.6458ⁿ`). Прямо из определений

```
Θ(Λ)/δ(Λ) = (2R/λ₁)ⁿ = ρⁿ,    Θ ≥ 1   ⇒   ρ ≥ δ_max(n)^{−1/n}
```

По Кабатянскому—Левенштейну `δ_max(n) ≤ 2^{−(0.599+o(1))n}`, то есть
асимптотически `ρ ≥ 2^0.599 = 1.5147`. Эйзенштейнову шагу нужно `ρ ≤ 1.52753` —
**окно шириной 0.85 %** над абсолютным препятствием. Отсюда видно, почему
`7^{n/2}` существует только в исключительных размерностях `n = 2,4,6,8,24`
(где `ρ = √2` или меньше) и не может быть общим асимптотическим механизмом,
тогда как `ρ ≤ 2` достижимо всюду — это и есть `3ⁿ` Лармана—Роджерса.
Отрицательные результаты проекта для `K₁₂` (`ρ = 1.633`) и для эйзенштейнова
ранга 5 (`ρ = 1.764`) — частные проявления той же тесноты.

---

## Кампания 07.08.2026 (поздний вечер): полосный сертификат двумерного слоя, ℝ¹⁰ ≤ 28812

Кандидаты слоевого ламинирования (см. кампанию «вечер» ниже) упирались в
верхнюю оценку диаметра. Прямое обобщение кусочного сертификата на слой ранга 2
требовало бы общего измельчения **трёх** сдвинутых разбиений Вороного E₈ —
~10⁵ кусков. Редукция (`core/layer_certificate.py`) обходит это целиком:

- для z в треугольнике Делоне слоевой решётки (вершины vₖ, описанный радиус
  R_L, сторона s) минимум по трём слоям ≤ минимума по двум ближайшим;
- для пары (k,l) задача расщепляется: вдоль ребра — ровно одномерная
  двухслойная задача (`piecewise_covrad.certify_two_layer`) с высотой s и
  сдвигом c_l−c_k; поперёк, на области, где эти две вершины ближайшие
  (треугольник vₖv_l o), отклонение ≤ инрадиуса R_L/2. Итого
  `R² ≤ max TwoLayer(c_l−c_k, s) + R_L²/4`;
- редукция ничего не теряет: при нулевых сдвигах возвращает ровно продуктовое
  R₀² + R_L²; по ℤ[ω]-эквивариантности три разности сдвигов — единичные кратные
  одной (g, ωg, −ω²g), достаточно одного вызова; интервал высот дополнительно
  режется на 8 полос с сужающимся окном z.

Результаты (`results/dim10_certificate.json`; D_min и порог √7 — как в
кампании «вечер», горизонтальные векторы закрыты планарной теоремой):

| индекс | N | a | серт. диаметр | d серт. | вердикт |
|---:|---:|---:|---:|---:|---|
| **28812** | 12 | 0.95 | 2.535952016 | **1.043297** | **проходит — новая оценка** |
| 28812 | 12 | 1.10 | 2.638186988 | 1.002867 | проходит |
| 21609 | 9 | 1.15 | 2.644491007 | 1.000477 | формально проходит; запас 4.8·10⁻⁴ — держим кандидатом до рационализации |
| 21609 | 9 | 1.05 | 2.604313401 | 0.990348 | не проходит |

Итог: **χ(ℝ¹⁰,[1,1.043297]) ≤ 28812** — статус [Ч] повышенной пробы (как у
7203); лучше 3¹⁰ в 2.05 раза и лучше строгой ступени 45619 на 37 %. Индекс
38416 (измеренное d = 1.0616) сертификатом не прогонялся. Статья: §8,
подсекция «Слоевое ламинирование в размерности 10».

## Кампания 07.08.2026 (вечер): продуктовое исчисление ширин и ℝ¹⁰ ≤ 45619

Ширина `d = D/diam` до сих пор была отчётом о качестве раскраски. Оказалось, это
**расходуемый ресурс с точным законом сложения**, и он сразу даёт новую оценку в
размерности 10.

**Продуктовое исчисление** (`core/product_calculus.py`). Для ортогональной суммы
`Λ = ⊕Λᵢ`, `Γ = ⊕Γᵢ` ячейка — произведение ячеек, поэтому `diam² = Σ diamᵢ²`, а
`D(v)² = Σ Dᵢ(vᵢ)²` при `Dᵢ(0) = 0` — минимум достигается на одном ненулевом
блоке. Исключение свободных масштабов блоков сворачивает условие допустимости в
одно неравенство только по ширинам:

```
Σᵢ 1/dᵢ² ≤ 1,        ℓ = (Σᵢ 1/dᵢ²)^(−1/2),        цветов = Πᵢ kᵢ
```

Прежнее правило `Σρᵢ² ≤ 4` — частный случай при `Γᵢ = 3Λᵢ` (`dᵢ = 2/ρᵢ`). Общая
форма допускает блоки **разного индекса**, и именно это работает.

**Результат: χ(ℝ¹⁰, [1, √(217/214)]) ≤ 2401 · 19 = 45619** против прежних
`3¹⁰ = 59049` (выигрыш 22.7 %). Оба блока — уже доказанные величины:

- `E8 / (3+ω)E8`, индекс `7⁴ = 2401`, `diam² = 6`, `D = √7` (планарная теорема
  плюс равенство на минимальных векторах) ⇒ `d₁² = 7/6`, цена `6/7`;
- `aA₂ / (5+2ω)aA₂`, индекс `N(5+2ω) = 19`. В плоскости ячейка Вороного **и есть**
  шестиугольник, так что `D` точен: ближайшая к `(5+2ω)/2 = (2, √3/2)` точка
  шестиугольника — вершина `(1/2, 1/(2√3))` на расстоянии `√(31/12)`, откуда
  `d₂² = 31/4` и цена `4/31`.

`6/7 + 4/31 = 214/217 < 1`. Проверено сквозным построением решётки в ℝ¹⁰:
индекс ровно 45619, `D_min = 2.645751311` (= √7), `diam = 2.627399057`,
`d = 1.006984951` совпадает с `√(217/214)` до 10⁻⁹
(`campaigns/dim10_product.py`, `results/dim10_product.json`).

**Индекс 19 в плоскости неулучшаем** (`campaigns/ladder2d.py`). Полный перебор:
все формы Эрмита индекса `k` (их `σ(k)`) × все формы плоских решёток с точностью
до подобия (двупараметрическое семейство, мультистарт Нелдера–Мида). Порог
`d ≥ √7` впервые берётся при `k = 19`; `k = 16, 17, 18` дают 2.598, 2.520, 2.479.

Нижняя часть диапазона закрыта **доказательно, двумя дополняющими экранами**.
К объёмному (`k ≥ vol(V₀ ⊕ (ℓ·diam/2)B)/det`, пол 15.574 на шестиугольной)
добавился новый **инрадиусный экран** (`core/minkowski.py: inradius_floor`):
из `B(0,λ₁/2) ⊆ V₀` следует `D(v) ≤ |v| − λ₁`, значит `λ₁(Γ) ≥ ℓ·diam + λ₁`, а
неравенство Эрмита `λ₁(Γ)ⁿ ≤ γₙ^(n/2)·det Γ` превращает это в
`k ≥ (ℓ·diam + λ₁)ⁿ / (γₙ^(n/2)·det Λ)`. Он **не следует** из объёмного и в
плоскости при `ℓ = √7` сильнее: 16.443 против 15.574 на шестиугольной решётке.
Оба верны для каждой формы, поэтому строгий пол есть `min` по формам от
`max` двух экранов: скан пространства модулей плюс доводка дают **16.021**
(при `x = −0.5, y = 1.0754`), то есть **k ≥ 17**. Остаются численными только
`k = 17` и `k = 18`.

**Динамика по разбиениям размерности** (`campaigns/product_ladder.py`): база
лестниц (α-лестницы для ℤ, ℤ[i], ℤ[ω], гурвицевых кватернионов + сертифицированные
результаты проекта) и минимизация `Π kᵢ` при `Σ 1/dᵢ² ≤ 1`. Таблица
**воспроизводит все известные рекорды проекта** (n = 4:45, 5:132, 6:343, 7:1029,
8:2401, 9:7203, 12:3¹², 24:7¹²) — это её проверка — и добавляет три новых
значения: `n=10: 45619`, `n=25: 4·7¹² = 55 365 148 804` (против `3²⁵`, в 15.3
раза), `n=26: 19·7¹² = 262 984 456 819` (против `3²⁶`, в 9.7 раза).
Данные: `results/product_ladder.json`.

**α-лестница.** Единая закрытая форма для всех четырёх порядков, действующих
подобиями: если `Λ` — модуль над `R ∈ {ℤ, ℤ[i], ℤ[ω], H}` и `α ∈ R`, то
`Γ = αΛ` имеет индекс `|α|ⁿ` и `D(αw) ≥ 2|w|·dist(α/2, U)`, где `U` — ячейка
Вороного `Rw` (отрезок, квадрат, шестиугольник, 24-ячейка). Отсюда
`d ≥ 2·dist(α/2,U)/ρ`. Вещественный множитель даёт `d = (m−1)/ρ`, эйзенштейнов
`3+ω` — `√(7/3)/ρ`; **гурвицев кватернион нормы 7 даёт ровно ту же константу
`√(7/12)`**, а норма 8 (`2+2i`) — `√(1/2)`, то есть требует `ρ ≤ √2` при индексе
`8^(n/2) = 2.828ⁿ`.

**Отрицательное: 7⁶ в ℝ¹² невозможно.** Планарная теорема даёт лишь нижнюю
оценку `D ≥ √(7/3)λ₁`, и был шанс, что у K₁₂ настоящее `D` больше (тогда
`χ(ℝ¹²) ≤ 7⁶ = 117649` вместо `3¹² = 531441`). Прямой счёт: перебор 85 680
векторов `(3+ω)K₁₂` против полупространств **всех 756** минимальных векторов даёт
`D_min = 3.055050463` — ровно планарный пол, при нужных `diam = 2√(8/3) =
3.265986`. Вершина A₂-шестиугольника лежит внутри `V₀(K₁₂)`: её могли бы отсечь
только векторы нормы `< 1.1547λ₁`, а таких у K₁₂ нет. Значит эйзенштейнов путь в
ℝ¹² требует именно `ρ ≤ √(7/3) = 1.5275`, а у K₁₂ `ρ = 1.6330`.

**Новый метод — ламинирование решёточным слоем** (`core/layer_lamination.py`).
Ламинирование проекта надстраивает по одному измерению слоями `tℤ`; прямая —
худшее из возможных покрытий. Слоевая **решётка** `L` размерности `m` покупает
`m` измерений за один `R(L)²`. Отсюда **теорема о бюджете**: раскраска базы с
шириной `d₀` допускает любой слой с

```
R_L ≤ (diam₀/2)·√(d₀² − 1)
```

то есть *избыток ширины раскраски — это в точности бюджет радиуса покрытия на
дополнительные измерения*. Для `E8/2401` бюджет ровно `R_L ≤ 1/2`: либо одна
прямая единичной высоты (ℝ⁹, индекс `2401·4 = 9604`), либо один шестиугольник с
`λ₁ = √3/2` (ℝ¹⁰, индекс `2401·19 = 45619`) — обе известные строгие ступени
оказываются двумя способами потратить один и тот же бюджет.

Конструкция сделана `ℤ[ω]`-эквивариантной: `ω` переставляет три минимальных
направления `A₂`, поэтому вместо трёх независимых слоевых условий остаётся одна
орбита, а свободных параметров — восемь (одна комплексная 4-компонентная
поправка), ровно как в кампании ℝ⁹. Это подняло численный фронтир с 0.85
(неэквивариантный поиск, 16 параметров) до 0.966 при том же бюджете.

**Кандидаты в ℝ¹⁰ (статус «измерено», как у 7203 до сертификата).**
*(Разрешено кампанией «поздний вечер» выше: 28812 сертифицирован и стал
оценкой, 21609 — на грани разрешения float, 38416 не прогонялся.)* За пределом
(P1) измеренный диаметр сильно меньше границы, и индекс падает:

| индекс | N | a | D_min | diam (P1) | diam измер. | d измер. |
|---|---|---|---|---|---|---|
| 38416 | 16 | 0.90 | 2.6457513 | 2.660827 | 2.492221 | **1.0616** |
| 28812 | 12 | 0.95 | 2.6457513 | 2.683903 | 2.514622 | **1.0521** |
| 28812 | 12 | 1.10 | 2.6457513 | 2.759227 | 2.614320 | **1.0120** |
| 21609 | 9 | 1.15 | 2.6457513 | 2.786276 | 2.621290 | **1.0093** |
| 21609 | 9 | 1.05 | 2.5791758 | 2.733130 | 2.574298 | **1.0019** |
| 21609 | 9 | 0.90 | 2.3779114 | 2.660827 | 2.491018 | 0.9546 |

Нижняя строка семейства — `21609 = 3²·7⁴`, в **2.73 раза** ниже `3¹⁰`; порог там
берётся с запасом 0.9 %, у 28812 — 5.2 %, у 38416 — 6.2 %. В трёх верхних
конфигурациях `D_min` упирается ровно в `√7`, то есть слоевые векторы уже
догнали горизонтальные и единственное, что отделяет их от теоремы, — граница на
диаметр.

Проверка на артефакт занижения радиуса (восхождение идёт снизу, поэтому редкая
выборка направлений завысила бы `d`): при 400 / 1500 / 5000 восхождениях
измеренный диаметр меняется меньше чем на `3·10⁻⁴` и `d` стабильно —
21609 → 1.002374 / 1.002294 / 1.002294, 28812 → 1.022111 трижды подряд,
38416 → 1.061730 / 1.061604 / 1.061604. Значит кандидаты не следствие
недосэмплированного восхождения; недостаёт именно верхней оценки диаметра.

Путь к сертификату известен
точно и является прямым обобщением `piecewise_covrad.certify_two_layer`: для
слоевой решётки, у которой ячейки Делоне вписаны в сферы радиуса `R_L` (верно для
`A₂` и для `ℤ`), барицентрические веса `w` точки `z` в её ячейке дают тождество
`Σ wₖ|z − vₖ|² = R_L² − |z − o|²`, откуда

```
R(Λ)² ≤ R_L² + max_x max_T Σ_{k∈T} wₖ · f(x − cₖ),     f(y) = dist(y, база)²
```

— вместо одного худшего значения `R₀²` из (P1) стоит **взвешенное среднее** `f`
по вершинам ячейки Делоне, и выбор поправок `cₖ` — это ровно занижение этого
среднего. Сертификация сводится к восьмимерному кусочно-выпуклому максимуму на
общем измельчении `|T|` сдвинутых разбиений Вороного (у проекта уже сделан
случай двух слоёв). Данные: `results/dim10_layer_*.json`.

### Слой ранга 2 в ℝ¹²: отрицательно, и это объясняет, где настоящее узкое место

Естественное продолжение — надстроить над `E₈/2401` слоевую решётку **ранга 2**
(четыре измерения сразу) и получить `χ(ℝ¹²) ≤ 2401·N(α)²`: при `N(α) = 12` это
345744, при `N(α) = 9` — 194481 против `3¹² = 531441`. Не проходит, и причина
оказалась содержательной.

**Лучшая эйзенштейнова решётка ранга 2 найдена:** CMA по эрмитовым формам 2×2
даёт `ρ = 1.3626553` (`λ₁² = 1`, целующее число 6, ковобъём 0.549414) — **лучше
D₄** (`√2 = 1.41421`) и A₂⊕A₂ (`1.63299`); контроль на `ℤ[ω]²` воспроизвёл
`2√(2/3)` точно. Для слоя связывает именно `λ₁(L)/R_L = 2/ρ(L)`, так что это
правильный оптимизируемый функционал.

**Но фронтир встал на 0.79–0.94.** При масштабе 0.95 — `d = 0.7939`, при 1.15 —
`0.9407`, однако там измеренный диаметр уже `2.7194 > √7`, то есть первыми падают
*горизонтальные* векторы и потолок `√7/diam = 0.973 < 1`. Пятикратное увеличение
бюджета CMA (3500 проб × 3 сида вместо 700 × 2) сделало **хуже** — `1.812` против
`2.026`: ландшафт рваный, и дело не в недооптимизации.

**Причина — плотность оболочек слоевой подрешётки.** Прямой подсчёт классов,
которые поправки обязаны вытолкнуть за `diam` одновременно:

| конструкция | λ₁(Γ_L) | окно | оболочки в окне | классов |
|---|---|---|---|---|
| ℝ¹⁰, слой ранга 1 (A₂, α=3) | 3.4500 | 3.9700 | 3.4500 ×6 | **6 = одна ω-орбита** |
| ℝ¹², слой ранга 2 (ρ=1.3627, α=4+2ω) | 3.2909 | 3.8560 | 3.2909/3.2913/3.2916/3.7077, по ×6 | **24 = четыре орбиты** |

В плоскости следующая оболочка после `3.45` — это `5.98`, далеко за окном, так
что на восемь параметров поправки приходится **одно** условие. В ранге 2 три
орбиты почти вырождены по длине (`3.2909`, `3.2913`, `3.2916`) и должны быть
вытолкнуты на одно и то же расстояние одновременно — четыре условия на
шестнадцать параметров. Отношение «условия на параметр» удваивается, и условия
почти вырождены.

Здесь же видна **тензия, которую раньше не с чем было заметить**: малое `ρ`
означает «круглую» решётку, а круглая решётка имеет много векторов почти равной
длины, то есть много связывающих орбит. Слою нужны одновременно малое `ρ`
(бюджет) и **разреженные оболочки** (выполнимость), а в ранге 1 это даётся
даром — двумерные оболочки разрежены сами по себе.

**Отсюда переприоритизация.** Бюджет слоя равен `(diam₀/2)·√(d₀²−1)`, и у
`E₈/2401` при `d₀ = 1.0801` он ровно `1/2` — этого хватает точно на один
шестиугольник и не хватает ни на что ранга ≥ 2. Если бы в размерности 8
существовала раскраска индекса ≈ 2800–3800 с шириной `d₀ ≈ 1.15–1.25`, бюджет
вырос бы в 1.6 раза (`R_L ≈ 0.81`), слой ранга 2 прошёл бы при `N(α) = 9` и
дал бы `≈ 3000·81 = 243000` в ℝ¹². То есть **пробел в восьмимерной лестнице
между 2401 (d=1.0801) и 6561 (d=1.4142) — общее узкое место сразу для
размерностей 10–16**, а не только для ℝ¹⁰. Эйзенштейново-диагональное семейство
его закрыть не может: h-лестница груба (0.5, 0.7638, 1.0), между 0.7638 и 1.0
пусто.

### Пробел лестницы ℝ⁸: деформацией E₈ не закрывается

Пробел между `2401` (`d = 1.0801`) и `6561` (`d = 1.4142`) оказался общим узким
местом размерностей 10–16, поэтому стоит спросить прямо: нельзя ли поднять
ширину самого блока `2401`, взяв вместо `E₈` другую эйзенштейнову решётку?
Отображение выигрыша прямое, `d = √(7/3)/ρ`, и пороги в ℝ¹⁰ такие:

| ρ ≤ | d ≥ | плоский проставок | индекс ℝ¹⁰ |
|---|---|---|---|
| 1.409903 | 1.083460 | 16 | 38416 |
| 1.358117 | 1.124745 | 13 | 31213 |
| 1.322876 | 1.154701 | 12 | 28812 |
| 1.247219 | 1.224745 | 9 | 21609 |

У `E₈` ровно `ρ = √2 = 1.414214`, то есть **до первого порога не хватает 0.3 %**,
а объёмный пол упаковки `(1/δ₈)^{1/8} = 1.1868` ничего из таблицы не запрещает.
Всё это было бы **строго** — никаких измеренных диаметров.

Ответ отрицательный. Случайные старты CMA в пятнадцатимерном пространстве
эрмитовых форм ранга 4 не отвечают ни на что (`E₈` — слишком особая точка, они
дают `ρ ≈ 1.69`), поэтому опыт поставлен локально: `E₈` восстановлена из своего
`ℤ[ω]`-базиса (тетракодовая тэта-конструкция → эрмитова форма → Холецкий),
воспроизведён `ρ = 1.414213562` ровно, и CMA запущена **из неё** с малым шагом.
Все восемь прогонов (четыре сида × два шага) дают только хуже: 1.419816,
1.423660, 1.424281, 1.424066 при `σ = 0.02` и 1.460166, 1.440803, 1.435086,
1.437388 при `σ = 0.08`.
**`E₈` — локальный минимум `ρ` среди эйзенштейновых решёток ранга 4.**

Две методические заметки, обе стоили прогона: (1) параметризовать надо
множитель Холецкого, а не эрмитову форму — при нужных размерах внедиагональных
элементов почти каждая проба теряет положительную определённость, и CMA видит
плоский штраф вместо градиента; (2) контроль обязателен — `deep_hole` идёт к `R`
снизу и занижённое `ρ` подарило бы фантом, но на `E₈` он даёт `√2` уже при 220
восхождениях и не меняется до 3000, так что измерение в 8D надёжно.

Вывод для плана: пробел лестницы ℝ⁸ придётся брать **неэйзенштейновыми
подрешётками** `E₈` индекса 2800–3800, а не деформацией родительской решётки.

## Кампания 07.08.2026: кусочный сертификат диаметра, ℝ⁷ ≤ 1029, ℝ⁹ ≤ 7203, K₁₂ в ℝ¹²

Итоги однодневной кампании по плану аудита `journal/REVIEW-2026-08-07.md`.

**Три новых теоремы (доказательства в статье):**

- **Планарная теорема** (`prop:planar`): `D((3+ω)Λ) ≥ √(7/3)·λ₁` для *всех*
  эйзенштейновых решёток — проекция на плоскость `span(w, ωw)` шести
  полупространств равной нормы даёт шестиугольник A₂, и плоское расстояние
  считается явно. Закрывает «≥» тождества `eq:eis` (открытый вопрос 7),
  переводит `D_min = √7` конструкций 343/2401/ламинирования из численного
  факта в теорему и делает строгой ширину `χ(ℝ²⁴,[1,√(7/6))) ≤ 7¹²`
  (классическая часть — АБПР). Повекторная проверка: 26 640 векторов E₈,
  2 466 E₆*, точность 10⁻¹¹ (`results/planar_inradius_checks.json`).
- **Полное доказательство `prop:mult`**: нижняя половина
  (`D ≥ (m−1)λ₁` для всех векторов) — из опорного полупространства.
- **Инрадиусная лемма** (`lem:inradius`): `D(v) ≤ |v| − λ₁`. Следствие: при
  `ρ < 2` ни одна подрешётка строго между `3Λ` и `Λ` не годится — тяжёлый скан
  n=10 (59 048 классов, 0 выживших) становится двухстрочным следствием.

**Новый инструмент — кусочный сертификат диаметра слоёной решётки**
(`core/piecewise_covrad.py`). Ограничение минимума слоями {0,1} и общее
измельчение двух сдвинутых разбиений Вороного: на куске `V(0)∩(V(μ)+c)`
разность `A₁−A₀` **линейна**, кусок режется двумя гиперплоскостями
`|A₁−A₀| ≤ t²` на три области, где максимум по высоте — **выпуклая**
квадратика (`φ`, `A₀+t²`, `A₁+t²`), т.е. достигается в вершинах. Диаметр —
конечный максимум явных квадратик в явных вершинах. Калибровка: индекс 12005
(точное `R² = 1.619025` из точности (P1)) → сертификат `1.619026`
(разница = технический зазор 10⁻⁶). Секунды в 6D, ~1 мин в 8D.
Два предыдущих подхода (ветвление по S⁸, перечисление Делоне) и коробочный
B&B с колпаком `f ≤ R₈²` остаются в архиве как не сработавшие.

**Новые верхние оценки (все диаметры сертифицированы кусочным методом,
дискретная часть — прежний конвейер Финке–Похста с двойной проекцией):**

| результат | было | детали |
|---|---|---|
| **χ(ℝ⁷,[1,1.032881]) ≤ 1029** | 1323 | ламинирование E₆*/343, m=3, t=27/25 (CMA с сертификатом в целевой); все слоевые > √7, связывает пол-теорема; сертиф. диаметр 2.561526 |
| **χ(ℝ⁹,[1,1.016591]) ≤ 7203** | 9604 | кандидат t=1.15 разрешён: сертиф. диаметр 2.602571007 < √7 (LP-насыщение совпало до 7 знаков) |
| **χ(ℝ⁹,[1,1.058127]) ≤ 9604** | ширина 1.0098 | сертиф. диаметр 2.500410 при t=0.93 |
| **χ(ℝ¹²,[1,√(3/2)=1.224745]) ≤ 3¹²** | ширина 1.0755 | Γ=3K₁₂: ρ(K₁₂)=√(8/3); λ₁²=4 и det=3⁶ в точной арифметике, D_min — по prop:mult |

Лестница ℝ⁹ пересертифицирована и перестроена: `7203→1.016591`,
`9604→1.058127` (ступени 12005/14406 доминированы), далее `16807→1.062345`,
`19208→1.066688`, `24010→1.070453`, `28812→1.073621`, предел √(7/6).

**Почему 1029 вообще возможен:** порог «m ≥ 4» был выведен из опытов над E₈;
все масштабные инварианты базы E₆*/343 те же (d'=√(7/6), ρ=√2), но структура
глубоких дыр другая — слоевое семейство i=3 останавливается на 2.5878 > 
сертифицированного диаметра. Строгий режим (P1) при t=1 даёт лишь 0.978 —
CMA-потолок; решает именно сертификат.

**Отрицательные итоги кампании (честно):** (а) CMA по эрмитовым формам ранга 5
(цель ρ ≤ √(7/3) → 7⁵ в ℝ¹⁰): лучший ρ ≈ 1.82 от ℤ[ω]⁵, сечение K₁₂ даёт
1.73 — до цели 1.5275 далеко, генус-перебор остаётся открытым путём;
(б) сечения K₁₂ для n=10,11 хуже башни (1.936/1.732); (в) строгий режим
t ≤ 1 для 1029 упёрся в 0.978 (аналог 9D-несовместимости, но мягче).
Данные: `results/dim7_laminate_*`, `results/dim9_*_piecewise.json`,
`results/dim12_k12.json`, `results/rho_scan_9_12.json`,
`results/eisenstein_rank5_cma.json`, `results/dim7_certified_push.json`.

**Статус строгости:** все новые оценки — [Ч] повышенной пробы: плавающая
точка сведена к значениям явных рациональных квадратик в вершинах явных
политопов (qhull) и к QP-проекциям с двойной верификацией. Рационализация
уровня ℝ⁵/132 — следующий шаг; горизонтальные полы √7 уже теоремны
(планарная теорема).

## Размерности 10–12: башня ламинаций E₈ и раскраска Γ = 3Λ

Работают два факта.

**Правило вещественного множителя.** Для `Γ = m·Λ` (индекс `mⁿ`) минимум `D`
достигается на `m·u` с минимальным `u`: минимальные векторы всегда релевантны
по Вороному, поэтому ячейка доходит ровно до `λ₁/2` в этом направлении и
`D_min = (m−1)·λ₁`. При `m = 3` это `D_min = 2λ₁`, то есть

    d = 2/ρ,   ρ = diam/λ₁,   условие допустимости — ровно ρ ≤ 2.

(`m = 2` требовал бы `ρ ≤ 1` — невозможно выше размерности 1.) Правило сверено
с прямым измерением на двенадцати решётках, совпадение до шести знаков. Оно специфично для **вещественных** множителей: у `3+ω` минимум уходит в другое
место, и там работает эйзенштейново тождество `D² = (7/3)λ₁²`.

**Башня.** Ламинирование E₈ удерживает `ρ < 2` во всех размерностях. Слой
высоты `t` над глубокой дырой даёт `R_up(n+1)² = R_up(n)² + t²/4`, а `λ₁`
сохраняется, пока `R² + t² ≥ 1`. Две роли радиуса покрытия тянут в разные
стороны и их нельзя путать: высоту надо брать из **нижней** оценки `R` (иначе
слоевой вектор слишком короток и `λ₁` незаметно падает ниже 1), а диаметр
распространять по **верхней**. Подстановка одной и той же оценки в обе роли —
это и есть способ получить внешне строгую, но неверную рекурсию.

При `t² = 1 − R_low²` рекурсия остаётся ниже неподвижной точки `diam² = 4`,
поэтому `ρ < 2` и `d > 1` при всех `n ≥ 8`.

| n | индекс 3ⁿ | diam ≤ | D_min | d ≥ (строго) | d (измерено) |
|---:|---:|---:|---:|---:|---:|
| 9 | 19 683 | 1.581138830 | 2 | 1.264911064 | 1.264911 |
| 10 | **59 049** | 1.695582496 | 2 | **1.179535649** | 1.224745 |
| 11 | **177 147** | 1.791182105 | 2 | **1.116581052** | 1.154701 |
| 12 | **531 441** | 1.859659467 | 2 | **1.075465716** | 1.154701 |

Неравенством здесь является только `R_up`: `λ₁ = 1.000000000` проверяется
напрямую через `shortest_vector`, а `D_min` — исчерпывающим перечислением всех
векторов `3Λ` нормы `≤ 2·diam` (272/336/432/4080 штук, все проверены двумя
независимыми процедурами проекции — Дейкстрой и SLSQP). Строка n = 9 приведена
для контроля: собственная конструкция размерности 9 (9604) много сильнее.

Прямые суммы дают заметно худшие интервалы при том же индексе: E₈⊕A₂ → √(6/5) =
1.0954 (n = 10), E₈⊕A₃\* → √(12/11) = 1.0445 (n = 11), E₈⊕D₄ → ровно 1 (n = 12);
для прямой суммы `ρ²` складывается, и условие вырождается в `Σρᵢ² ≤ 4`.

**Можно ли опустить индекс ниже 3ⁿ.** Подрешётки между `3Λ` и `Λ` — это
подгруппы `H ⊆ Λ/3Λ = (ℤ/3)ⁿ` индекса `3ⁿ/|H|`; `H` допустима, только если у
каждого ненулевого класса `min D ≥ diam`. Каждый класс имеет представитель нормы
`≤ R(3Λ) = 3R`, поэтому шар такого радиуса встречает их все. Скан при n = 10
(945 276 векторов, 59 048 проекций) даёт **0 выживших классов из 59 048**: уже
минимальный представитель каждого класса запрещён. Скан вёлся с **заниженной**
оценкой diam, поэтому класс объявлялся плохим только тогда, когда он плох и для
истинного диаметра — вывод корректен: **3¹⁰ оптимален среди подрешёток,
содержащих 3Λ**. Подрешётки, не содержащие `3Λ`, этим не исключены; для них
нужен полный поиск по `|F| ≈ 4·10⁶`, что вне бюджета.

**Отрицательное по эйзенштейнову пути.** Индекс `7^{n/2}` требует `ρ ≤ √(7/3) =
1.5275`. В комплексном ранге 5 (размерность 10) лучший тернарный код длины 5 с
`d ≥ 3` имеет `k = 2` ([5,3,3]₃ не существует — граница Синглтона), решётка даёт
`ρ = (2/3)√7 = 1.7638`; прямое измерение `D_min = √7` ровно, `d = 0.8367`.
Вместе с `K₁₂` (`ρ = √(8/3)`) это распространяет частичный отрицательный ответ
на открытый вопрос 2 АБПР с ранга 6 на ранг 5.

Скрипт и данные: `campaigns/dim10_12_tower.py`,
`results/dim10_12_tower.json`.

## Размерность 9: 17253 → 9604 ламинированием E₈/2401

Опубликованная оценка АБПР `17253` получена из `A₉*`. Новый объёмный скрин
(`chromatic_research/core/minkowski.py`) показал, что она стоит в **10.5 раза**
выше строгого препятствия Минковского `vol(V₀ ⊕ R·B)/det Λ`, тогда как все
остальные размерности проекта стоят в 1.4…4.0 раза:

| n | родитель | граница Минковского | известный k | k / граница |
|---|---|---:|---:|---:|
| 2 | A₂ | 4.51 | 7 | 1.55 |
| 3 | A₃* | 10.42 | 15 | 1.44 |
| 4 | D₄ | 28.88 | 49 | 1.70 |
| 5 | A₅* | 55.55 | 140 | 2.52 |
| 6 | E₆* | 127.86 | 343 | 2.68 |
| 7 | E₇* | 340.2 | 1372 | 4.03 |
| 8 | E₈ | 663.9 | 2401 | 3.62 |
| 9 | A₉* | 1639.7 | 17253 | **10.52** |

Вывод скрина: размерности 9 не хватало не оптимизации, а конструкции. Она
построена ламинированием (`core/lamination.py`, кампания
`campaigns/dim9_laminate.py`): слои `E₈` на высоте `t` со сдвигом `c`,
характер `(3+ω)` поднят склейкой `a ∈ 𝔽₇⁴` и модулем слоя `m`; индекс `2401·m`.

Работают два неравенства, доказываемых в одну строку:

- **(P1)** для точки `(x,z)` берём ближайший слой `i` (`|z−it| ≤ t/2`), тогда
  `dist² ≤ R(E₈)² + t²/4`, то есть `diam(Λ) ≤ √(diam(E₈)² + t²)`;
- **(P2)** `E₈×{0} ⊂ Λ` даёт `V₀(Λ) ⊆ V₀(E₈)×ℝ`, поэтому для горизонтальных
  `v` (то есть `v ∈ (3+ω)E₈`) выполнено `D_Λ(v) ≥ D_{E₈}(v) = √7`.

Значит запас `d(E₈/2401) = √(7/6)` — ровно бюджет на рост ячейки, а численно
надо проверять только векторы с ненулевой слоевой координатой; их единицы,
поскольку `D(v) ≥ |v| − diam` автоматически закрывает всё длиннее `2·diam`.

| индекс | m | t | склейка a | diam (строго) | diam (изм.) | D_min | d строго | d изм. |
|---:|---:|---:|---|---:|---:|---:|---:|---:|
| **9604** | 4 | 0.93 | (5,1,4,5) | 2.6200954 | 2.5004094 | √7 | **1.009792** | 1.058127 |
| 9604 | 4 | 1.00 | (3,3,5,2) | 2.6457513 | 2.5251007 | √7 | 1.000000 | 1.047781 |
| **12005** | 5 | 0.69 | (5,6,6,4) | 2.5448183 | 2.5448183 | √7 | **1.039662** | 1.039662 |

Первая строка — усиление, найденное отдельно: при `t = 1` строгая величина равна
ровно `1` (интервал вырожден), но `diam ≤ √(6+t²)` верно при **любом** сдвиге, так
что при `t < 1` строгий диаметр уже строго меньше `D_min = √7` — вопрос лишь в
том, проходят ли слоевые векторы. CMA по сдвигу со **строгим** диаметром в
знаменателе (`dim9_laminate refine`, тот же приём, что дал 7203) показывает, что
проходят: при `t = 0.93` достигается `d ≥ 1.009792`, и это ровно потолок
`√7/diam`. Ниже связывают слои (`0.983` при t=0.90, `0.945` при 0.86, `0.932`
при 0.82), выше — потолок (`1.008419` при 0.94, `1.005647` при 0.96), так что
`t = 0.93` — точка пересечения и строгий оптимум. В этой цепочке **нет ни одной
оценки радиуса покрытия**: `diam ≤ √(6+t²)` — однострочный (P1), `D_min = √7` —
(P2) для 240 горизонтальных векторов плюс 26 слоевых, пересчитанных Дейкстрой и
SLSQP. Итог: `χ(ℝ⁹,[1,1.0097]) ≤ 9604`.

Статусы двух строк различаются, и это важно. Для **12005** сдвиг — глубокая
дыра, измеренный диаметр совпадает с границей (P1) до 15 знаков, поэтому
`d ≥ 1.039662` не зависит от численной оценки радиуса покрытия: запас в 4 %
строгий, и вместе с ним `χ(ℝ⁹,[1,1.039]) ≤ 12005`. Для **9604** сдвиг смещён
с орбит глубоких дыр — это разрушает «дважды глубокую дыру» (точку, глубокую
сразу для двух соседних слоёв) и опускает настоящий диаметр ниже границы (P1).
Строгая величина при `t = 1` равна ровно `1.000000`: этого достаточно для
классической оценки `χ(ℝ⁹) ≤ 9604` (ровно как `d = 1` у BCC/15 в ℝ³), но
невырожденный интервал `[1, 1.0478]` опирается на измеренный диаметр, а
LP-оценка радиуса покрытия сходится **снизу**, то есть могла бы его занизить.
Понижение `t` ниже 1 при этих `c` и `a` не помогает: слоевые векторы сразу
становятся связывающими (`t = 0.98` даёт уже `0.99952`). У E₈ **две орбиты
глубоких дыр**, и выбор орбиты существенен: при индексе 12005 они дают
`1.0397` против `0.8863`.

Проверка (`dim9_laminate verify`): точный целочисленный определитель индекса;
перечисление Финке–Похста всех векторов подрешётки нормы `≤ 2·diam` (266 при
12005, 256 при 9604), из них горизонтальные закрыты (P2), а слоевые (26 и 16)
пересчитаны **двумя независимыми процедурами проекции** — Дейкстрой из combigeo
и SLSQP; согласие до 11 знаков. Точный рациональный сертификат уровня
ℝ⁵/132 и ℝ⁷/1323 — следующий шаг, пока это вычислительное свидетельство.

### Строгая лестница ширин в ℝ⁹

У проекта есть лестницы ширин для ℝ³–ℝ⁶, а для ℝ⁹ была известна одна точка.
Ламинирование даёт её целиком, и притом строго: индекс `2401·m`, высота `t`
подбирается так, чтобы слоевые векторы ровно проходили, и тогда связывает
горизонтальный потолок, то есть `d = √7/√(6+t²)` — **равенство**, а не оценка.

| индекс | m | t | `d` | статус |
|---:|---:|---:|---:|---|
| 9604 | 4 | 0.93 | 1.009792 | строго |
| 12005 | 5 | 0.69 | 1.039662 | строго |
| 14406 | 6 | 0.55 | 1.053883 | строго |
| 16807 | 7 | 0.45 | 1.062345 | строго |
| 19208 | 8 | 0.39 | 1.066688 | строго |
| 24010 | 10 | 0.33 | 1.070453 | строго |
| 28812 | 12 | 0.27 | 1.073621 | строго |
| — | ∞ | → 0 | → √(7/6) = 1.080123 | предел |

Каждая строка — оценка `χ(ℝ⁹,[1,ℓ]) ≤ k` при `ℓ < d`. Все ступени, кроме первой,
берутся уже на сдвиге в глубокую дыру, и там граница (P1) **точна** (существует
точка, глубокая сразу для двух соседних слоёв), поэтому верификатор даёт
`d(строго) = d(измеренное)` до последнего знака — величины не оценены, а
вычислены. Первая строка требует смещения с орбит глубоких дыр и разобрана
отдельно ниже.

Предел лестницы содержателен: при `m → ∞` высота `t → 0`, решётка вырождается в
`E₈ × ℝ`, и ширина стремится к `√(7/6)` — ровно к ширине самой конструкции
`E₈/2401` в размерности 8. Ламинирование интерполирует между рекордом ℝ⁸ и ℝ⁹.
Данные: `results/dim9_ladder_m{5,6,7,8,10,12}{,_verified}.json`.

### Ниже 9604 в строгом режиме: исчерпывающий отрицательный экран

При `t = 1` граница (P1) даёт `diam ≤ √7` **точно**, поэтому запрещённое
множество, построенное против этого диаметра, делает строгой любую найденную
против него раскраску — и искать можно по **всем** подрешёткам, а не только по
семейству `2401·m`. Множество построено: `|F| = 498 084` (42 мин),
`results/.search-cache/forbidden-lam9604-rigorous-v1.npz`.

**Общий поиск** (`prime_radon`, модули только из `{2,3,5}` — FFT размера `7⁹`
неподъёмна) при десяти индексах `6480…9000` не дал ничего; минимум неотделённых
векторов `killed = 4` при `6561 = 3⁸`. Показательно, что чисто 3-адические
структуры идут заметно лучше смесей (`killed` 4 и 7 против 14–40): у решётки
эйзенштейнова структура, и `3 = N(1−ω)` для неё выделено.

**Исчерпывающий экран.** Если зафиксировать четыре характера mod 7 решётки
`(3+ω)E₈`, остаётся отделить лишь `F'_a = {f : ψ(f) + f₉·a ≡ 0 mod 7}` — медиана
**168** векторов вместо 498 084. Тогда индекс `7203` достигается **любым**
функционалом mod 3, а не только строкой «`y₉ mod 3`» ламинирования, и всё
пространство перебирается полностью: 2401 склейка × 9841 проективная форма.

| |G| | индекс | результат |
|---|---:|---|
| 2 | 4802 | **невозможно** (полный перебор) |
| 3 | **7203** | **невозможно** (полный перебор, 0 попаданий) |
| 4 | 9604 | достижимо (ℤ/4, склейка (0,0,1,3)) |
| 5 | 12005 | достижимо (ℤ/5) |

Проверены обе группы порядка 4 (`ℤ/4` и `ℤ/2×ℤ/2`). Воспроизведение известных
`9604` и `12005` служит положительным контролем машинерии, так что отсутствие
`7203` — именно невозможность, а не неудача поиска.

Вывод: **на этой родительской решётке 9604 — точный пол в строгом режиме.**
Данные: `results/dim9_below9604_screen.json`.

### Индекс 7203 (m = 3): кандидат, статус не определён

CMA по 8-мерному сдвигу слоя (`dim9_laminate refine`) при фиксированной высоте
доводит `m = 3` до отношения выше 1 — независимо на трёх высотах:

| t | d (измерено) | diam (измерено) | потолок √7/diam |
|---:|---:|---:|---:|
| 1.15 | **1.016592** | 2.602570 | 1.016592 |
| 1.22 | 1.016247 | 2.603454 | 1.016247 |
| 1.26 | 1.010029 | 2.619480 | 1.010029 |

Отношение всюду совпадает с потолком, то есть все слоевые векторы уже проходят
и связывает **горизонтальное** ограничение. Дискретная часть безупречна: точный
целочисленный определитель `7203`, `D_min = √7` ровно, 240 горизонтальных
векторов закрыты доказательством (P2), 56 слоевых пересчитаны Дейкстрой и SLSQP.

**Но оценкой это не является.** Всё утверждение сводится к одному числу —
диаметру, а он зажат между двумя оценками, и порог лежит строго внутри:

    LP (снизу)   2.602570238  ≤  diam  ≤  2.706011826   (P1, строго)
                        порог √7 = 2.645751311

LP-оценка насыщена: одно и то же значение при `n_dirs` 800, 6000, 15000 (два
сида) и воспроизведено независимым прогоном на другой высоте — это довод, что
глобальный максимум найден, но не доказательство. Обратный пример из этой же
кампании показывает цену вопроса: при `t = 1.30` бюджет `n_dirs = 150` дал
`d = 1.00164`, а пересчёт с бюджетом 800 — `0.99684`; **то пересечение было
артефактом заниженного радиуса покрытия.**

Строгий коридор закрыт с другой стороны. При `t ≤ 1` граница (P1) сама даёт
`diam ≤ √7`, но там связывают слоевые векторы: CMA даёт максимум `0.9402`
(t = 1.00), `0.9387` (t = 1.05), `0.9040` (t = 0.95). То есть для `m = 3`
требование по слоям (`t ≳ 1.15`) и строгая граница по диаметру (`t ≤ 1`)
**несовместимы** — рычага внутри схемы (P1) не осталось.

Двухслойное усиление (P1) тоже не помогло: `R² ≤ max(φ(0), φ(r₂²))` с
`φ(u) = u + ((R₈²−u+t²)/(2t))²` потребовало бы `r₂² ≤ 1.30`, а измерение даёт
`r₂² = 1.5 = R₈²` для **обеих** орбит порядка 2 — добавление одного класса
смежности вообще не уменьшает радиус покрытия E₈.

Поэтому доказанной оценкой остаётся **9604**, а `7203` — кандидат, для которого
не хватает ровно одного: сертифицированной верхней границы радиуса покрытия
решётки в размерности 9.

#### Два метода сертификации радиуса, которые не проходят (проверено)

**Ветвление по направлениям.** `R = max_u h(u)` по опорной функции, и для конуса
над симплексом с единичными вершинами `W` (строки) и значениями `h` верна оценка
`max_i h_i / σ`, где `σ = dist(0, conv W)`. Соблазн усилить её до `‖W⁻¹h‖`
(точный максимум `hᵀλ/‖Wᵀλ‖` по Коши–Буняковскому) **неверен как рычаг**:
отбрасывание `λ ≥ 0` даёт супремум по всем λ, а у мелких симплексов строки `W`
почти параллельны и граница уходит в бесконечность — ровно там, где нужна
точность. С корректной `max h/σ` выигрыша от отсечения нет: чтобы подтвердить
`R ≤ ρ` при `ρ/R = 1.0166`, нужно угловое разрешение `≤ 21°` **всюду** по `S⁸`,
то есть ~3·10⁷ конусов. Метод экспоненциален по существу, не по реализации.

**Перечисление ячеек Делоне** (с проверкой полноты по объёму — единственный
известный строгий сертификат). Разведка: 8000 восхождений дают **7279 различных**
вершин ячейки, то есть новая вершина примерно на каждом старте; полное число
отвечает решётке общего положения (`9! = 362 880` ячеек Делоне). Полный перебор
потребует ~10⁶–10⁷ восхождений. Qhull на 752 фасетах в размерности 9 не
завершается вовсе.

Побочный, но существенный результат разведки: максимум `|x| = 1.301285119`
**не сдвинулся** при 200, 500, 1000, 2000, 4000 и 8000 стартов, пока
обнаруживались тысячи новых вершин. Вместе с насыщением LP-оценки и
воспроизведением на другой высоте это делает `diam = 2.602570238` весьма
правдоподобным — но доказательством не является, и статус `7203` остаётся
«кандидат». Данные: `results/dim9_laminate_m3_candidate.json`,
`results/dim9_laminate_m3_verified.json`.

Почему ламинирование выигрывает только в 9: подъём стоит множителя `m ≥ 4`, а
`4·343 = 1372 > 1323` в ℝ⁷, `4·132 = 528 > 343` в ℝ⁶, `4·1323 = 5292 > 2401`
в ℝ⁸. Размерность 9 — единственная, где своя конструкция была настолько хуже
соседней.


Подробный отчёт о том, что и как проверялось: границы, достигнутые численно,
отрицательные экраны и их честный статус. Итоговые доказанные оценки — в
[README.md](README.md), полное изложение — в статье
[`paper/chi4-45.tex`](paper/chi4-45.tex).

Все результаты, не снабжённые точным сертификатом, — вычислительные наблюдения,
а не доказательства невозможности.

## Размерности 5 и 7: спуск до 132 и 1323

- Новый determinant-aware exact-repair, active-set trust-region, полный поиск
  по первичным CRT-блокам и гибридный all-values оракул для больших простых
  множителей; новый пороговый bitmask-оракул исчерпывает дискретную часть
  фиксированной метрики. Расширение портфеля при индексе 134 обошло прежний
  локальный потолок `0.995366`; затем проверки сразу на −1, −2 и −3 цвета
  нашли и точно сертифицировали ещё более сильное ядро индекса 132. Уже от
  нового рубежа тем же конвейером проверены индексы 131, 130 и 129. Новый
  цикл «Pareto-архив → мостовая метрика → полный проективный оракул →
  деформация → active-set» поднял фронтир 131 с `0.987424` до `0.990216`;
  все `296 765 305` строк над `F₁₃₁` перечислены двумя сборками оракула.
  Для 130 и 129 получены исчерпывающие фикс-метрические скобки
  `[0.9766881,0.9766882)` и `[0.9570141,0.9570142)`. Ни один из этих
  отрицательных результатов не является доказательством невозможности после
  изменения метрики. Экраны в
  ℝ⁶, ℝ⁸, ℝ⁹ сохранены как ориентиры, а не как доказательства невозможности.

## Размерности 6, 8, 9: экраны без пробоя порога

Ближайшие индексы ниже известных оценок в размерностях 8 и 9 дополнительно
проверены новым ленивым разделяющим оракулом и симметрийным orbit-CEGAR.
Строгого улучшения там пока нет: лучшие фиксированные отношения составили
`0.76425` для `E₈/2400` (после деформации) и `0.82584` для
деформированной `A₉*/17250`. Протоколы, точные индексы и следующий алгоритм:
[`audit-data/README-dim5-9.md`](audit-data/README-dim5-9.md) и
[`NEXT_MECHANISM.md`](audit-data/NEXT_MECHANISM.md).
Для \(\mathbb R^6\) арифметическое продолжение точного фактора
\((\mathbb Z/7)^3\) к индексу `336=7·4·4·3` оказалось существенно сильнее
локальных ремонтов 342/340. Совместный поиск ядра и формы поднял полный
численный фронтир сначала до `0.980658185310`, а точные переходы через
L-type-стены и последовательный SDP/cutting-plane шаг — до
`0.984244122033`. Разрыв до новой оценки 336 сократился до 1.576%, но порог
1 пока не пересечён. Внешняя LP-аппроксимация PSD-конусов собственными
векторами через открытый HiGHS проверила по 95 L-type-ветвей на трёх глубинах,
а дискретно-непрерывный турнир довёл 169 HNF-различных ядер; максимум
`0.984244122202` численно неотличим от прежнего в пределах рабочих допусков.
Отдельная согласованная оптимизация индексов 329, 322 и 315 дала лишь
`0.944303`, `0.949097` и `0.933317`; лучший новый нециклический остров
`333=37·3·3` остановился на `0.961088`. Периодический не-косетный подъём
индекса 7 также не прошёл необходимый слой назначений. Полная тороидальная
модель с произвольными независимыми множествами затем проверила через
HiGHS/CP-SAT 16903 независимо сгенерированных периодов порядков 686--2401, 1798 гладких
периодов индексов 344--1026 и ещё 1151 период на деформированной форме;
требуемого независимого множества не найдено. Эти портфели не исчерпывают
все периоды. Для вложенных простых подъёмов результат теперь исчерпывающий:
точный bitset-оракул проверил все 23941 проективных характера при
`p=2,3,5,7`, включая все 3231 и 19536 неполных случая при `p=5,7`.
Независимая Fourier-редукция SDP Ловаса к LP HiGHS дала
\(\vartheta=p\) на всех 56 профильных представителях; вместе с точным
\(\alpha=p\) это влечёт и \(\vartheta'=p\).

Проверена и принципиально иная 342-цветная ветвь: цвет объединяет
последовательные остатки циклического фактора. Для пар остатков совместная
оптимизация строки и формы (CMA-ES, LP HiGHS и SDP) остановилась на
`0.9212415444`; блоки размеров 3--6 оказались ещё слабее, лучший результат
`0.9002398182`. Полные MIP остались `UNKNOWN`, поэтому это не запрет всей
семьи и не новая оценка. Доказанная оценка здесь остаётся `343`. Протокол и
честные статусы численных и отрицательных экранов:
[`journal/RESEARCH_2026-07-30.md`](journal/RESEARCH_2026-07-30.md).
