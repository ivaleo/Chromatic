# Комплексный аудит проекта Chromatic — 21 июля 2026

Аудит по трём осям (корректность/отлаженность, быстродействие, системность) для трёх
подпроектов (`voronoi` → пакет voronoi4d, `combigeo` C++17+pybind11, `chromatic` фасад),
плюс сверка со статьями из `articles/` и вычислительная верификация опубликованных
оценок хроматических чисел. Все вычисления воспроизводимы: скрипты и сырые данные — в
`audit-data/`.

**Соглашения.** Λ — решётка, Γ ⊂ Λ — подрешётка индекса k = |det M| (k цветов —
классы смежности), D = min по v ∈ Γ\{0} расстояний между одноцветными ячейками
Вороного, D(v) = 2·dist(v/2, V₀) (лемма Иванова), d = D/diam(V₀); раскраска даёт
χ(ℝⁿ, [1, ℓ]) ≤ k при ℓ ≤ d (граничный случай d = 1 требует аккуратной раскраски
границ — полуоткрытые ячейки, ср. Prop 5 у Arman et al. и Кулсона).

---

## 1. Резюме

1. **Математическое ядро корректно, все тестовые наборы зелёные** (7/7 C++ ctest,
   29+13+14 pytest). Оба движка независимо (GJK по вершинам против каскада проекций)
   воспроизводят флагманский результат **D₄, k=49 → d = √(7/6) = 1.080123** —
   combigeo за 0.5 с, voronoi4d за 322 с; найденные оптимальные подрешётки совпадают
   по d.
2. **Опубликованные результаты воспроизведены полностью** (§2): обе таблицы Иванова
   (ℝ² — 7/7 строк точно, ℝ³ — 5/5, включая совпадение его вершин/диаметра до 6
   знаков), Кулсон (15 и 21 цвет в ℝ³), Радоичич–Тот (A₄*/54), Arman–Bondarenko–
   Prymak–Radchenko (49/140/343/2401 в n = 4/5/6/8). Попутно найдены и исправлены
   **три опечатки в матрицах подрешёток** в статьях Иванова.
3. **Получены новые результаты** (§2.3): точные константы интервалов для всех
   конструкций Arman et al. (нигде не публиковались), эмпирический закон
   d = √(7/3)/ratio для подрешётки (3+ω)Λ, второй 4-мерный свидетель оценки 49
   (решётка K₃,₃ с тем же d = √(7/6)), поддержка гипотезы о минимальности 49 на всех
   16 комбинаторных классах 4-мерных решёток, продолжения таблиц Иванова.
4. **Отлаженность**: ни одного неверного опубликованного числа не найдено (флагман
   выдержал 6000 адверсариальных сверок с независимым QP-решателем), но строгость двух
   реализаций различна. У эталонного voronoi4d — три критических пробела
   (неполный by design перебор HNF: на k=49 покрыто 96% подрешёток; окно ±limits без
   границы достаточности; ячейка без валидации — на скошенном базисе молча мусор), из-за
   чего его результаты — корректные нижние оценки без сертификата оптимальности;
   исчерпывающий перебор combigeo эти сертификаты восстанавливает. У combigeo главный
   дефект интерфейсный — `transition` в скрытой внутренней LLL-системе координат
   (доказан контрпример), плюс heap-overflow в python-`lll_reduce` на рваном входе и
   масштабозависимые допуски. Направление всех численных погрешностей — **вверх по
   d**, т.е. в сторону ложной «пригодности» — важно для результатов на границе d = 1.
5. **Быстродействие**: C++ ядро быстрое (~4 мкс/подрешётку, 3.48 млн подрешёток за
   14.8 с), но диапазонные запуски теряют ~15–50% на перestройке ячейки и сбросе кэша
   между индексами, и 4 LLL-приведения на подрешётку вместо одного; крупнейший
   алгоритмический резерв — фактор-редукция HNF-перебора по группе симметрий решётки
   (для D₄ |W(F₄)| = 1152).
6. **Системность**: единая терминология выдержана (num_colors/diameter/normalized),
   но контракт терминологии (корневой README) не под версионным контролем, в
   voronoi/README — ошибка формулы (лишний множитель 2), `article.tex` не
   компилируется и содержит черновой мусор, у combigeo нет LICENSE, в папке articles —
   два дубликата, канонической гексагональной решётки A₂ нет ни в одном пресете.

---

## 2. Верификация научных результатов

### 2.1. Воспроизведённые результаты литературы

| Результат | Источник | Наш расчёт | Статус |
|---|---|---|---|
| χ(ℝ²) ≤ 7; χ_[1,ℓ](ℝ²) ≤ 7 при ℓ ≤ √7/2 | Исбелл; Иванов 2011, Т.2 стр.1 | A₂, k=7: d = √7/2 = 1.322876; k=7 — минимальное допустимое в 2D | ✓ точно |
| Таблица Иванова для ℝ² (k = 7, 9, 12, 13, 16, 19, 21 → √7/2, √3, 2, √19/2, 3√3/2, √31/2, √37/2) | Иванов, Вестник РУДН 2011 (= дисс. 2011) | все семь значений совпали точно; вдобавок доказана их **оптимальность** среди всех подрешёток данного индекса | ✓ |
| χ(ℝ³) ≤ 15 | Coulson 2002; Радоичич–Тот | BCC, k=15: d = 1.000000 ровно (D = diam = √5) — граничный случай, реализуемый полуоткрытыми ячейками, как у Кулсона | ✓ |
| χ_d(ℝ³) ≤ 21 при d < √(7/6) | Coulson 1997 (цит. по Иванову) | FCC, k=21: d = √(7/6) = 1.080123 | ✓ точно |
| Таблица Иванова для ℝ³ (k = 18, 21, 23, 24, 27 → 1.115, 1.133, 1.137, 1.303, 1.549) | Иванов 2011, Т.3 | 1.115838 / 1.133698 / 1.137320 / √(17/10) = 1.303840 / √(12/5) = 1.549193; для k=18 его диаметр 2.261514 и расстояние 2.523483 совпали до 6 знаков; его подрешётки k=18, 23 — оптимальны | ✓ |
| «Почти правильная» 6-раскраска плоскости, 0.99144 | Иванов §5.2 | 0.991445 на его спецрешётке, подтверждена оптимальность | ✓ |
| Разрежённые раскраски (d < 1): ℝ² k=3,4,6; ℝ³ k=4,5,6,7,8,12,14 | Иванов, Т.4–5 | все границы подтверждены (k=3: 1/2; k=4: √3/2; ℝ³ k=4: √(1/10), k=8: √(3/5) и т.д.) | ✓ (k=14 — см. §2.2) |
| Вспомогательные: ℤ² 8 цветов, ℤ³ 26 цветов | Иванов §5.1 | обе дают d = 1.000000 ровно | ✓ |
| χ(ℝ⁴) ≤ 49 | Иванов, УМН 61:5 (2006); Arman et al., Th. 3 | D₄, k=49: d = √(7/6), оба бэкенда независимо | ✓ |
| 49 — минимальный индекс для D₄ (Prop 12) | Arman et al. (arXiv:2112.13438) | полный перебор k = 2..60: единственное допустимое k = 49 (140050 подрешёток на индексе, счётчик совпал с теоретическим Σd₂d₃²d₄³) | ✓ |
| χ(ℝ⁴) ≤ 54 через A₄*; 54 минимально для A₄* | Радоичич–Тот 2003; Arman et al., Prop 12 | A₄*, первое допустимое k = 54: d = √(23/20) = 1.072381 | ✓ |
| χ(ℝ⁵) ≤ 140 (явная C₅) | Arman et al., Th. 1 | A₅* с их матрицей C₅: d = √(39/35) = 1.055597 ≥ 1; ячейка — пермутоэдр f = [720, 1800, 1560, 540, 62] | ✓ |
| χ(ℝ⁶) ≤ 343 через (3+ω)E₆* | Arman et al., Th. 8 | d = √(7/6) = 1.080123 (их гарантия была лишь ≥ 3/(2√2) ≈ 1.0607); радиус покрытия E₆* подтверждён численно (ratio² → 2.000) | ✓ |
| χ(ℝ⁸) ≤ 2401 через (3+ω)E₈ | Arman et al., Th. 8 | E₈ (тетракод-конструкция: 240 минимальных векторов, 120 релевантных пар — все пины сошлись): d = √(7/6) | ✓ |
| f-векторы ячеек | классика | 24-ячейка [24,96,96,24], тессеракт, ромбододекаэдр [14,24,12], усечённый октаэдр [24,36,14], пермутоэдры A₄* [120,240,150,30] и A₅* | ✓ |

Дополнительная взаимная проверка: независимый numpy/scipy-конвейер (QP по
полупространствам вместо GJK по вершинам), написанный для 6D/8D, откалиброван на
D₄/49 и совпал с combigeo до 9 знаков.

### 2.2. Найденные расхождения в литературе (все — опечатки в статьях, не ошибки кода)

1. **Иванов, ℝ², строка k=19**: напечатанная подрешётка (3 2; 7 −1) имеет det 17 ≠ 19
   (в статье и диссертации одинаково). Граница √31/2 верна; корректная матрица (в
   базисе его решётки): наша HNF даёт эквивалент [[1,2],[−3,1]]-семейства с det 19.
2. **Иванов, ℝ³, строка k=21**: напечатанная матрица имеет det 12 ≠ 21. Граница 1.133
   верна: полный перебор на его решётке diag(1; −0.246656) даёт оптимум d = 1.133698 с
   восстановленной матрицей **[[3,0,0],[0,1,5],[0,0,7]]**.
3. **Иванов, разрежённые ℝ³, строка k=14**: напечатанная матрица (1 0 3; 0 1 7; 0 0 14)
   даёт лишь d = 0.732 < заявленных 0.876. Граница верна: оптимум его решётки
   diag(1; −0.147065) равен d = 0.876365 с матрицей **[[1,0,12],[0,1,10],[0,0,14]]**.
4. Мелкие: обрезанная координата вершины C в его таблице вершин (−1,04699 →
   −1,046995); несогласованность матрицы 6-раскраски между §5.2 (det 3) и Т.4 (det 6 —
   верная); в статье 2011 ссылка на УМН-заметку 2006 с искажённой пагинацией.

### 2.3. Новые результаты и наблюдения

**(а) Точные нормированные константы опубликованных конструкций** (в статьях не
печатались; каждая даёт новую интервальную оценку χ(ℝⁿ, [1, ℓ]) ≤ k при ℓ ≤ d):

| n | k | Решётка | d точно | d ≈ |
|---|---|---|---|---|
| 4 | 49 | D₄ | **√(7/6)** | 1.080123 |
| 4 | 54 | A₄* | **√(23/20)** | 1.072381 |
| 4 | 56 | A₄* | **√(7/5)** | 1.183216 |
| 5 | 140 | A₅* (C₅ из Arman et al.) | **√(39/35)** | 1.055597 |
| 6 | 343 | (3+ω)E₆* | **√(7/6)** | 1.080123 |
| 8 | 2401 | (3+ω)E₈ | **√(7/6)** | 1.080123 |

Отдельно про A₄*/56: это лучшая найденная ширина интервала в 4D при ≤ 60 цветах —
шире, чем у 49-цветной (√(7/5) > √(7/6)); компромисс «число цветов ↔ ширина
запретного интервала», которого нет в литературе.

**(б) Закон конструкции Эйзенштейна.** Для α = 3+ω (|α|² = 7) на решётке Λ с
эйзенштейновой структурой выполняется (проверено точно в n = 2, 4, 6, 8):

>  D(αΛ)² = (7/3)·λ₁(Λ)², т.е. **d = √(7/3) / (2R/λ₁)** — нормированный интервал
>  зависит только от отношения покрытие/упаковка.

Для решёток с ratio = √2 (D₄, E₆*, E₈, Λ₂₄) это даёт ровно √(7/6); для A₂
(ratio = 2/√3) — ровно √7/2. Следствие-гипотеза (проверка для Λ₂₄ вычислительно
тяжела): **χ(ℝ²⁴, [1, √(7/6)]) ≤ 7¹²**.

**(в) Новые свидетели оценки χ(ℝ⁴) ≤ 49.** Скан всех 16 представителей комбинаторных
классов 4-мерных решёток (по формам из Vallentin–Weißbach–Zimmermann 2024), k = 2..54:

- **ни одна решётка не допустима ниже k = 49** — свежее вычислительное свидетельство
  в пользу гипотезы Arman et al. (OQ1), что 49 минимально среди всех решёточных
  раскрасок ℝ⁴ (наши точки — представители классов; непрерывная оптимизация по
  вторичным конусам остаётся открытой);
- на k = 49 допустимы четыре: **K₃,₃-решётка с тем же d = √(7/6)** (новый, геометрически
  иной свидетель 49: f-вектор ячейки [102,216,144,30] против [24,96,96,24] у 24-ячейки),
  111⁻ (ровно d = 1), 211⁺ (1.00965), 221⁺ (1.01192);
- корневая A₄ не допустима до k = 65 (макс. d = √(35/36) ≈ 0.986 — «почти»);
- ℤ⁴ впервые достигает d = 1 лишь при **k = 80** (не 81 = 3⁴, как в классической
  кубической конструкции).

**(г) Продолжения таблиц Иванова** (оптимальные значения среди подрешёток данного
индекса; прямой ответ на его «списки можно продолжить и далее»):

- ℝ², гексагональная решётка: новые ступени лестницы max-d: **k=25 → 2√3 ≈ 3.464;
  k=27 → 7/2; k=28 → √13 ≈ 3.606** (k = 22, 23, 24, 26, 29, 30 роста не дают).
- ℝ³, классические решётки (без деформаций): **k=20 → √(11/10) ≈ 1.0488 (BCC)** —
  между 15 (d=1) и 21 (√(7/6)) в литературе значений не было; далее k=24 → √(17/10)
  (BCC), k=27 → √(12/5) (BCC), k=30 → √(19/10), k=32 → √(5/2), k=39 → √(14/5),
  k=40 → √(33/10) (все BCC).

Матрицы-свидетели ключевых результатов (целочисленные, в координатах входного
базиса, det = k):

- D₄, k=49: [[0,1,0,2],[1,−3,2,0],[0,−3,0,1],[−2,0,3,2]]
- A₄*, k=54: [[1,−3,2,1],[1,0,−1,−2],[−1,−2,0,1],[−2,1,1,−3]]
- A₄*, k=56: [[1,−3,3,0],[0,0,−1,−2],[−2,−1,1,2],[3,−2,0,2]]
- K₃,₃, k=49: [[0,1,3,0],[0,−2,1,0],[−2,0,0,−3],[−3,−1,−3,−1]]
- BCC, k=15: [[1,3,−3],[−3,−2,3],[0,1,−3]]; BCC, k=20: [[1,3,−3],[−1,−1,−1],[−2,1,2]]
- FCC, k=21: [[1,0,2],[−3,1,2],[−1,−2,3]]; A₂, k=7: [[1,2],[−3,1]]

**(д) Ориентиры снизу** (для контекста, из статей папки): χ(ℝ²) ≥ 5 (де Грей),
χ(ℝ³) ≥ 6, χ(ℝ⁴) ≥ 9 (Экзу–Исмаилеску–Лим), χ(ℝ⁵) ≥ 9, χ(ℝ⁶) ≥ 11; для решёточных
раскрасок нижний порог Кулсона 2ⁿ⁺¹−1 (n=4: 31) — т.е. между 31 и 48 в 4D вопрос
закрыт нашим сканом только для 16 модельных метрик.

### 2.4. Идентификация статей в `articles/` (12 файлов → 10 работ)

| Файл | Работа |
|---|---|
| Иванов R2 R3.pdf | Л.Л. Иванов, «О хроматических числах ℝ² и ℝ³ с интервалами запрещённых расстояний», Вестник РУДН, 2011, №1, 14–29 |
| Диссертация - Иванов.pdf | Л.Л. Иванов, канд. дисс., РУДН, 2011 (гл. 2 = статья выше + χ(ℝ⁴) ≥ 7; гл. 3 — проблема Борсука) |
| 2112.13438v2.pdf | Arman, Bondarenko, Prymak, Radchenko, «Upper bounds on chromatic number of Eⁿ in low dimensions», arXiv:2112.13438 — **ключевая для проекта** (49/140/343/1372/2401/17253) |
| Journal_of_London_Math_Soc_2021_… | Dutour Sikirić, Madore, Moustrou, Vallentin, «Coloring the Voronoi tessellation of lattices», JLMS 104 (2021) — хроматические числа графов Вороного (инвариант касания, не метрический): χ(ℤⁿ)=2, χ(Aₙ)=χ(Aₙ*)=n+1, χ(Dₙ*)=4, χ(E₈)=16, χ(Λ₂₄)=4096 |
| THE CHROMATIC NUMBER OF 4-DIM. LATTICES 2024.pdf | Vallentin, Weißbach, Zimmermann, arXiv:2407.03513**v1** |
| ХЧ 4.pdf | **та же работа, v3** (дубликат по содержанию): 16 классов графов Вороного 4-мерных решёток, χ ∈ {2,…,7}, у D₄ χ=4 |
| ХЧ пространств.pdf ≡ elibrary_28107270….pdf | **побайтовые дубликаты**: Черкашин, Райгородский, «О хроматических числах пространств малой размерности», ДАН 2017 (нижние оценки) |
| otsenka_khromaticheskikh_chisel….pdf | Горская, Митричева, Протасов, Райгородский, Матем. сб. 2009 — асимптотические нижние оценки ζ_k (даёт нижний контекст для интервалов: χ(ℝⁿ,[1,√k]) ≥ (ζ_k+o(1))ⁿ) |
| 2018 хр число плоскости 5.pdf | de Grey, «The chromatic number of the plane is at least 5», 2018 |
| 1-s2.0-S0166218X19304172-main.pdf | Prosanov, «A new proof of the Larman–Rogers upper bound…», DAM 276 (2020) |
| гипотеза вороного.pdf | Гарбер, Магазинов, УМН 77:1 (2022) — гипотеза Вороного доказана при d ≤ 5 ⇒ решёточные ячейки Вороного аффинно исчерпывают параллелоэдры для всех размерностей проекта, кроме n=6 (там гипотеза открыта) |

---

## 3. Аудит: корректность и отлаженность

Общий статус: **все 63 теста трёх проектов проходят**; математика ядра выдержала
адверсариальную проверку (полнота HNF-перебора подтверждена совпадением счётчиков с
теоретической формулой Σ∏dⱼʲ на всех прогонах; окно кандидатов |v| < D₀ + diam
доказуемо полно при верной ячейке; лемма Иванова применена в законной области —
центрально-симметричные выпуклые ячейки).

### 3.1. combigeo (C++ ядро)

**Критично (интерфейс, не математика):**
- **`transition` в скрытой системе координат.** `find_optimal` внутри LLL-приводит
  входной базис (solver.cpp:80) и строит HNF относительно приведённого базиса
  (solver.cpp:97), который наружу не выдаётся. Доказано контрпримером (косые базисы
  ℤ²): `transition × входной_базис` порождает **другую** подрешётку. CLI печатает
  только эту матрицу. Скаляры (D, d, diam) корректны; однозначная идентификация —
  только `best.sub_basis` (объемлющие координаты). Фикс: пересчитывать HNF в базис
  пользователя или выдавать/печатать приведённый базис и `sub_basis`.

**Крупные:**
- **Heap-buffer-overflow (доказан ASan)** в python-`lll_reduce` на рваном списке
  (`[[1,0],[0,1,5]]`): единственная точка входа без `validated_dim` (bindings.cpp:92),
  а внутренние guard'ы — `assert`, отключённые NDEBUG. Противоречит USAGE.md о
  валидации в release.
- **Абсолютные допуски → масштабозависимая корректность**: гексагональная решётка
  ×10¹⁰ даёт молча неверную ячейку (f = [0,2]); валидная решётка ×10⁻⁶ отвергается как
  «вырожденная». Фикс: нормировка базиса по det^(1/n) на входе.
- **Окно ±3 при поиске релевантных векторов — эвристика без сертификата** (polytope.cpp:
  67–101, честно помечено в заголовке). Пропуск релевантного вектора даёт молча
  раздутую ячейку и завышенное d. Для всех наших прогонов f-векторы совпали с
  классикой (включая 5D-пермутоэдр), но для будущих кампаний n ≥ 5 нужен
  сертифицированный перебор (SphereEnum по смежному классу) + self-check Эйлера/объёма.
- **Направление ошибок**: GJK возвращает ≥ истинного расстояния (итерата внутри
  политопа), пропуск релевантного вектора тоже завышает d ⇒ систематический уклон к
  **ложной допустимости**; порог d ≥ 1 сравнивается без epsilon (cli/main.cpp:47).
  Для результатов ровно на границе (BCC/15, ℤ³/26 и т.п.) выводы подтверждаются
  аналитическими значениями, но в отчётности стоит помечать |d−1| < tol отдельно.

**Мелкие** (выборочно): stall/max_iter-выходы GJK без сертификата оптимальности;
`py::arg("from")` — зарезервированное слово Python (kwarg недоступен, в доках "frm");
переполнение/UB `lround` ключа кэша на экстремальных координатах; нет ограничения
размерности (n ≥ 32 — UB сдвига, глубокая рекурсия — segfault из Python); прогресс
печатается в stdout из библиотеки; тест-фреймворк без try/catch (падение одного
теста валит бинарь); `delta = 1.0` допускается в LLL без гарантии завершения.

**Тесты combigeo**: сильные для n ≤ 4 (аналитические пины f-векторов, диаметров,
расстояний, счётчиков HNF, GJK против замкнутых форм, кросс-проверка с voronoi4d, пин
1.080123); дыры: нет ни одного теста n ≥ 5 (даже smoke ℤ⁵), нет теста реконструкции
подрешётки из `transition` (поймал бы критикал), нет тестов масштаба, нет тестов
solver'а на нецелых базисах.

### 3.2. voronoi4d (эталонный python-пакет)

Геометрическое ядро корректно для хорошо приведённых базисов: D₄ даёт точную
24-ячейку, объём ячейки = |det|, флагманское `{49: 1.0801234497}` воспроизводится, и
**6000 адверсариальных сверок каскада проекций с независимым QP-решателем не нашли ни
одного завышения** в зоне d ≥ 1. Но у пакета есть три критических пробела строгости
(важно: они делают его результаты **корректными нижними оценками без сертификата
оптимальности/полноты**, а не ошибочными числами; для симметричных решёток итоговые
оптимумы совпали с исчерпывающим перебором combigeo):

**Критично:**
- **Перебор HNF неполон by design**: `compute_factorizations` сводит диагонали к
  отсортированным мультимножествам (factorization.py:46), т.е. перечисляются только
  неубывающие диагонали. Измеренное покрытие подрешёток ℤ⁴: k=2 — 8/15, k=3 — 27/40,
  k=12 — 3384/6200, **k=49 — 134456/140050 (96%)**. Для симметричных решёток (ℤ⁴, D₄)
  пропущенные семейства эквивалентны учтённым по симметрии (потому D₄/49 совпал с
  полным перебором), для генерических — это genuinely другие раскраски. Тест
  test_factorization.py:6–8 закрепляет дефект как норму. Фикс дёшев: +4% времени на
  k=49.
- **Окно кандидатов ±limits (по умолчанию 2) без границы достаточности**: критерий
  |v| < D_best + diam нигде не реализован; для δ=0.75-LLL базиса теоретически возможны
  релевантные коэффициенты 3–5. Флагманский результат перепроверен на limits=3,4 —
  стабилен, но каждое опубликованное d формально без сертификата полноты (плюс окно
  зависит от того, какой именно приведённый базис вернул бэкенд LLL — см. fpylll ниже).
- **Ячейка строится без валидации**: фиксированное и **несимметричное** облако
  коэффициентов `range(-3,3)` (polyhedra.py:212), LLL лишь «рекомендован» в доках.
  Продемонстрировано: скошенный базис даёт без предупреждений «ячейку» с объёмом
  0.295 при |det| = 0.02 (в 14.8× раздута). Дешёвые решающие проверки (объём ≈ |det|,
  центральная симметрия) отсутствуют.

**Крупные:**
- Отсечение `nearest_faces` — недоказанная эвристика, на которой висит сертификат
  допустимости (эмпирически чистая: 0 завышений на 53 280 реальных кандидатов);
  внутри неё `set.intersection` вместо объединения; пустое пересечение → возврат
  **inf**, причём при s = 0 это происходит всегда — и текущая корректность
  `find_optimal` **случайно опирается на этот inf** (нулевой вектор безусловно
  добавляется в кандидаты); для точек внутри ячейки функция возвращает мусор
  (маскируется фильтром диаметра, но `dist_to_s` — публичный API без оговорок).
- **fpylll-путь возмущает решётку**: округление до 3 знаков меняет |det| иррациональных
  базисов (продемонстрировано 0.0149071 → 0.0148613, 0.31%), δ = 0.99 против 0.75 у
  чистого python — два бэкенда считают разные числа; `delta`/`precision` частично
  игнорируются в зависимости от пути.
- Семантика `threshold < 1` сломана двумя зашитыми константами d≈1 (фильтр диаметра
  search.py:53–56 и ранний выход distances.py:161–163): sub-1 значения — усечённые
  верхние оценки (до 0.29 абсолютного завышения), пригодные только для решения
  «принять/отвергнуть» при threshold = 1.
- `Face2D._calculate_normal` без проверки ранга (guard мёртв — норма столбца
  ортогональной матрицы всегда 1): почти коллинеарные вершины → молча неверная
  нормаль; неиспользуемое поле `bias`.

**Тесты voronoi4d** (29, все зелёные): нормировка протестирована только на решётке, где
она сокращается (ℤ⁴, max_len = 2 — «×2/max_len» тождественно); f-вектор 24-ячейки,
diam = 2 и флагманское 1.080123 не запинены; ветви каскада (2-грань/ребро/внутренность)
и сравнение с независимым оракулом отсутствуют; часть тестов тавтологична или
закрепляет дефекты (C1, фильтр диаметра вместо threshold).

**Быстродействие voronoi4d**: k=2..50 ≈ 40–60 мин (с fpylll), ~0.94 мс на матрицу;
горячая точка — генерация точек-кандидатов чистым python-циклом (625 кортежей ×
np.dot) и полный pdist до первого минимума. Дешёвые ускорения: один matmul вместо
цикла (~10–30× на хотспоте), предфильтр |b₁| ≤ diam, сортировка кандидатов по |v|,
батчевый `find_simplex`, орбитная дедупликация — суммарно порядок величины без смены
алгоритма.

### 3.3. chromatic (фасад)

Критических дефектов нет: оба адаптера возвращают одинаковые величины (проверено
численно), конвенции согласованы (строки-векторы, левое умножение перехода,
верхнетреугольная HNF, diameter = 2·max|vertex|, порог d ≥ 1), обещание «явный выбор
бэкенда, без фолбэков» в коде выполнено.

**Крупные:**
- **Кросс-валидационный тест вырожден**: единственный тест `compare_backends` гоняет
  k=8, где оба бэкенда возвращают d = 0.0 — сравнивается 0.0 с 0.0; пин 1.080123 есть
  только в C++ тесте; при нуле установленных бэкендов весь набор «зелёный» (skip).
- **fpylll молча меняет семантику**: при установленном fpylll (как в этом venv)
  voronoi4d-путь LLL округляет каждый базис до **3 знаков** (precision=3) и
  игнорирует `delta` (фактический 0.99). Для нецелых базисов (√3/2 и т.п.) бэкенды
  анализируют разные решётки — ложные расхождения compare_backends. Нигде не
  документировано.
- **`transition` в разных системах координат у двух бэкендов** (обе — внутренние
  LLL-рамки) — сравнение матриц между бэкендами бессмысленно; не документировано.
- Асимметричный сентинел пустого результата (voronoi4d: transition=None против
  заполненного у combigeo при том же математическом ответе d=0).
- У combigeo **нет LICENSE** (корневой README утверждает MIT у всех трёх); в
  combigeo мёртвый абсолютный путь `/Users/mac/Documents/_My_code/Python/voronoi`
  (README:96 и tests/python/test_combigeo.py:16 — на чужой машине тесты падают
  ошибкой, а не skip).

**Мелкие**: `compare_backends` потребляет генератор и сравнивает вердикт допустимости
точным сравнением на границе d=1; extras `chromatic[combigeo]` декоративны (резолвятся
в PyPI, не в соседние папки); у скомпилированного модуля combigeo нет `__version__`;
`find_optimal_range` фасада не использует нативный диапазон; оценка времени в
`examples/cross_validate.py` занижена на порядок.

---

## 4. Аудит: быстродействие

Замеры (Apple Silicon, Release):

| Операция | Время | Комментарий |
|---|---|---|
| CLI `combigeo D4 49` (140050 подрешёток) | 0.57 с | ~4 мкс/подрешётку |
| `find_optimal_range(D4, 2, 50)` (3.48 млн) | 14.8 с | единственное допустимое k=49 |
| Построение ячейки D4 / Z4 / A5* | 42 мс / 290 мс / 1.6 с | C(m, n) переборов вершин |
| voronoi4d `find_optimal(D4, 49)` | 322 с | эталон, ~630× медленнее ядра |
| Полный скан 16 решёток 4D × k=2..54 | ~4.5 мин | этот аудит |

Куда уходит время диапазонного запуска: (1) ~14% (D4) до ~50× (решётки с большим m) —
пересборка идентичной ячейки и **сброс кэша расстояний на каждом индексе**
(solver.cpp:138–143), хотя оба зависят только от базиса; (2) **4 LLL + 3 Грама–Шмидта
на подрешётку** вместо одного (min_color_distance → shortest_vector → два
vectors_within, каждый заново приводит); (3) кэш на `std::map<vector<long>,double>` —
аллокация ключа на каждый lookup и лексикографические сравнения.

Рекомендации по ускорению (по убыванию эффекта):
1. Общая ячейка + общий кэш на весь диапазон индексов (тривиально и корректно).
2. Одно LLL-приведение подрешётки, прокинутое во все вызовы.
3. `unordered_map` с упакованным хэш-ключом.
4. **Фактор-редукция HNF-перебора по Aut(Λ)** (для D₄ порядок 1152 — порядковый
   выигрыш; самый крупный алгоритмический резерв).
5. Свидетель — из победившего прохода, без повторного перебора.
6. Релевантные векторы через SphereEnum с сертифицированным радиусом (одновременно
   закрывает и корректностный риск ±3-окна).

Масштабируемость: n=5 практичен (ячейка A₅* 1.6 с; один индекс k≈140 — минуты);
n=6 — ячейка генерической решётки C(126,6) ≈ 7·10⁹ переборов вершин — вне
досягаемости текущего алгоритма (наш E₆*-расчёт обошёл это QP-подходом без вершин);
для кампаний n=6 нужен инкрементальный перебор вершин (adjacency walk) вместо C(m,n).

---

## 5. Аудит: системность и слаженность

**Выдержано хорошо**: терминологическая таблица фасада (num_colors = det = индекс;
min_distance = D; normalized = d; threshold d ≥ 1) согласована во всех трёх проектах и
корневом README; конвенции (строки-векторы, HNF, LLL delta 0.75) едины; тестовые
наборы имеют одинаковую структуру; .venv общий; README-инструкции установки работают.

**Дефекты системного уровня:**
1. Корневой README — терминологический контракт воркспейса — **вне версионного
   контроля** (корень не git-репозиторий); у chromatic и combigeo нет remote (риск
   потери двух новейших проектов); CI нет нигде.
2. `voronoi/README.md:17` — формульная ошибка (d = 2·D/diam при D, уже включающем
   двойку) и внутренне противоречивый docstring `distances.py:70`; код всюду верен
   (d = D/diam). Единственный источник расхождения формул в документации.
3. `voronoi/docs/article.tex` — не компилируется (нет пакетов algorithm/geometry,
   `\item` вне списка), содержит задублированные `\maketitle`/`\title`, битые
   копипасты формул (строки 248–491), загадочную формулу d = 2(D+ε)/diam с
   непоясненным ε и коллизию обозначений (D — и расстояние, и диаметр). Как «теоретическое
   обоснование метода» файл своей роли пока не выполняет.
4. `articles/`: два дубликата (побайтовый и v1/v3) — стоит оставить по одному файлу с
   говорящими именами.
5. Пресеты: канонической **гексагональной A₂ нет нигде** (ни CLI, ни фасад), хотя это
   первый пример всей теории; нет реестра решёток (D₄ перенабран литералом в 6+ файлах).
6. Воспроизводимость: до этого аудита не было единой точки воспроизведения
   флагманских чисел (теперь: `audit-data/`); `results.txt` пишется в CWD и не
   трекается; эталонные значения нигде не зафиксированы в python-тестах.
7. Гигиена: 8.8 МБ бинарник `legacy/src/engine` в истории combigeo; `.gitignore`
   combigeo без `.pytest_cache/`; версия зашита строкой в фасаде и отсутствует у
   модуля combigeo.

---

## 6. Приоритетные рекомендации

1. **(Корректность интерфейса)** Пересчитать `transition` в базис пользователя (или
   всегда выдавать пару «приведённый базис + HNF») и добавить тест реконструкции
   подрешётки; печатать `sub_basis` в CLI.
2. **(Документация-математика)** Исправить формулу в `voronoi/README.md` и docstring
   `distances.py`; вычистить `article.tex` до компилируемого состояния с одной
   согласованной формулой d = D/diam (и явной оговоркой про границу d = 1).
3. **(Отлаженность)** Валидация входа в python-`lll_reduce`; относительные допуски
   через нормировку базиса; guard «ни одного бэкенда — fail, не skip»; настоящий
   кросс-тест на допустимом индексе (пин √(7/6) в python, помеченный slow — 49-й
   индекс combigeo проходит за секунду).
4. **(Эталон voronoi4d)** Восстановить статус эталона: перечислять все упорядоченные
   диагонали HNF (фикс стоит +4% времени на k=49), реализовать границу достаточности
   окна |v| < D_best + diam вместо фиксированного ±limits, валидировать ячейку
   (объём = |det|, центральная симметрия) и заменить пересечение на объединение в
   `nearest_faces` (аккуратно: текущая корректность случайно опирается на inf при
   s = 0 — сначала защитить фильтром нулевого вектора); запинить в тестах f-вектор
   24-ячейки, diam = 2 и 1.080123.
5. **(Перед кампаниями n ≥ 5)** Сертифицированный перебор релевантных векторов +
   self-check (Эйлер, объём ячейки = det Λ); smoke-тесты n=5 (ℤ⁵, D₅, A₅*).
6. **(Быстродействие)** Пункты 1–4 из §4; после Aut-редукции реально замахнуться на
   открытый вопрос OQ1 Arman et al. в n=6 (побить 343) — это главная научная цель,
   достижимая этим инструментом.
7. **(Системность)** git init в корне (README + audit-data под контроль), remotes для
   chromatic/combigeo, LICENSE в combigeo, дедупликация articles/, пресет A₂ и общий
   реестр решёток, fpylll-семантику документировать или чинить (precision, delta).
8. **(Научное оформление)** Новые константы §2.3(а), закон (3+ω) §2.3(б) и свидетель
   K₃,₃ §2.3(в) — готовый материал для заметки-продолжения; таблицы-продолжения
   Иванова §2.3(г) — прямой ответ на «списки можно продолжить».

---

## Приложение A. Сводные лестницы max d(k) (оптимум по подрешёткам, наш расчёт)

**ℝ² (бегущий максимум по ℤ², A₂; все ступени — A₂):** k=7 → √7/2; 9 → √3; 12 → 2;
13 → √19/2; 16 → 3√3/2; 19 → √31/2; 21 → √37/2; 25 → 2√3; 27 → 7/2; 28 → √13 ≈ 3.606
(k = 29, 30 роста не дают).

**ℝ³ (бегущий максимум по ℤ³, FCC, BCC):** k=15 → 1 (BCC); 20 → √(11/10); 21 → √(7/6)
(FCC); 24 → √(17/10); 27 → √(12/5); 32 → √(5/2); 39 → √(14/5); 40 → √(33/10)
(все BCC, если не указано).

**ℝ⁴:** D₄: 49 → √(7/6) (единственное допустимое ≤ 60; 52, 56, 60 → ровно 1);
A₄*: 54 → √(23/20); 56 → √(7/5); 61/64 → √(7/5); ℤ⁴: 80 → 1; A₄: нет до 65.

Полные таблицы: `audit-data/sweep_results.json`, `audit-data/verify2_results.json`.

## Приложение B. Воспроизведение

```bash
cd Chromatic && source .venv/bin/activate
./combigeo/build/combigeo D4 49          # флагман: d=1.080123, 0.6 с
python audit-data/sweep.py               # лестницы d(k): Z2 A2 Z3 FCC BCC Z4 D4 A4 A4*
python audit-data/verify2.py             # таблицы Иванова + скан 16 решёток 4D
python audit-data/verify_a5s.py          # A5*/140 (5D)
python audit-data/verify_e6s.py          # (3+ω)E6*/343 (6D, QP-путь без вершин)
python audit-data/verify_e8.py           # (3+ω)E8/2401 (8D, тетракод)
```

Тесты: `ctest --test-dir combigeo/build`; `python -m pytest` в каждом из voronoi,
combigeo (tests/python), chromatic.
